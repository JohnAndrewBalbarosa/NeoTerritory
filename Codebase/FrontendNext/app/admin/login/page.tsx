// /admin/login → unified Google sign-in. (/admin itself is the admin SPA entry, B2.1b-3.)
import SignInSurface from '@/components/SignInSurface';

export default function AdminLoginPage() {
  return <SignInSurface />;
}
