// /why and /why/* → 'learn' (legacy industry-panels page redirects to /learn).
import MarketingSurface from '@/components/MarketingSurface';

export default function WhyPage() {
  return <MarketingSurface surface="learn" />;
}
