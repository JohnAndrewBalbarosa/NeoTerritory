// /patterns/learn → student learning hub (category list). Auth-gated.
import LearnHubSurface from '@/components/LearnHubSurface';

export default function PatternsLearnPage() {
  return <LearnHubSurface />;
}
