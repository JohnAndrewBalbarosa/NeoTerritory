// /patterns/gof → 'patterns' (preserved alias back to the unified index, per D59).
import MarketingSurface from '@/components/MarketingSurface';

export default function PatternsGofPage() {
  return <MarketingSurface surface="patterns" />;
}
