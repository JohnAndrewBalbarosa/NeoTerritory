// /research and /research/* → 'docs' (previous name; redirects to /docs in the shell).
import MarketingSurface from '@/components/MarketingSurface';

export default function ResearchPage() {
  return <MarketingSurface surface="docs" />;
}
