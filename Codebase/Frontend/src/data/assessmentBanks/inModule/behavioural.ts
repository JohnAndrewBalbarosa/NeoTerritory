// Authored in-module conceptual questions for the Behavioural design-pattern
// modules. Each module carries exactly five MCQs, one per Bloom level
// (Remember→Evaluate); Create is the practical C++ task and is never authored
// here. Items focus on intent, responsibilities, object interaction, state
// transitions, request handling, traversal, and collaboration — not class
// naming. Prompts, scenarios, and code snippets are deliberately distinct from
// the formal Form A/B bank (same concepts, fresh wording). All items are draft.

import { imq, type InModuleBank } from './_shared';

const GOF = ['Gamma, Helm, Johnson, Vlissides (1994), Design Patterns'];
const MOD = ['CodiNeo Behavioural module content'];
const SRC = [...MOD, ...GOF];

export const IN_MODULE_BEHAVIOURAL: InModuleBank = {
  // ================================ Strategy ================================
  'behavioural-strategy': [
    imq('remembering', 'strategy-intent', 'easy',
      'A Strategy design groups several ___ behind one shared interface so the host can use any of them.',
      ['interchangeable algorithms', 'one-to-many notifications', 'state snapshots', 'grammar rules'], 0,
      'Strategy bundles interchangeable algorithms behind a common interface. The distractors describe Observer, Memento, and Interpreter.', SRC),
    imq('understanding', 'strategy-mechanism', 'moderate',
      'In a Strategy design, why does the host class keep a reference to the strategy interface rather than a concrete algorithm?',
      ['So the strategy can notify the host of changes', 'So that any concrete algorithm can be plugged in and delegated to without editing the host', 'So the host can inherit each algorithm in turn', 'So all algorithms share a single global instance'], 1,
      'Holding the interface lets the host delegate to any concrete algorithm interchangeably, so swapping behaviour never touches the host.', GOF),
    imq('applying', 'strategy-select', 'moderate',
      'A media exporter must encode video with H.264, VP9, or AV1, with the choice picked from a settings menu at runtime and the rest of the export pipeline untouched. Which approach fits?',
      ['Let an internal mode drive the encoder transitions', 'Notify a list of subscribers each time encoding finishes', 'Pull each encoder behind a shared interface and have the pipeline delegate to the one the caller selected', 'Wrap each encode request as a storable, undoable object'], 2,
      'Caller-selected, interchangeable algorithms behind one interface is Strategy. The distractors describe State, Observer, and Command.', GOF),
    imq('analyzing', 'strategy-identify', 'difficult',
      'Study this code:\n\nstruct TaxRule { virtual double apply(double amount) const = 0; };\nstruct FlatTax : TaxRule { double apply(double a) const override { return a * 0.2; } };\nclass Invoice {\n  const TaxRule* rule;\npublic:\n  explicit Invoice(const TaxRule* r) : rule(r) {}\n  double total(double net) const { return net + rule->apply(net); }\n};\n\nWhich pattern does Invoice’s collaboration with TaxRule demonstrate?',
      ['State — the rule changes the object’s mode', 'Template Method — a fixed skeleton with overridable steps', 'Command — the request is reified for undo', 'Strategy — the host delegates to an injected, interchangeable algorithm'], 3,
      'Invoice delegates the varying calculation to an injected TaxRule it never names concretely, with no state-transition methods — that is Strategy.', GOF),
    imq('evaluating', 'strategy-evaluate', 'difficult',
      'A class accepts a compression algorithm object through its constructor and delegates compression to it; the object never swaps itself out, and the caller decides which one to pass. Is labelling this Strategy defensible?',
      ['Yes — caller-chosen, interchangeable, delegated algorithms with no self-driven transitions is exactly Strategy', 'No — it must be State because behaviour is delegated to a polymorphic object', 'No — Strategy requires the host to inherit the algorithm', 'Yes — but only if there are at least three algorithms available'], 0,
      'Strategy is a caller-selected algorithm injected and delegated to; the absence of self-driven state transitions distinguishes it from State.', SRC),
  ],

  // ================================ Observer ================================
  'behavioural-observer': [
    imq('remembering', 'observer-intent', 'easy',
      'The Observer pattern sets up a relationship in which ___.',
      ['a request travels through handlers until one accepts it', 'one subject automatically notifies many dependents when its state changes', 'two abstractions vary independently', 'an object snapshots its state for later restore'], 1,
      'Observer establishes one-to-many automatic notification. The distractors describe Chain of Responsibility, Bridge, and Memento.', SRC),
    imq('understanding', 'observer-mechanism', 'moderate',
      'How do dependents in an Observer design find out that the subject changed?',
      ['They periodically poll a shared global flag', 'They inherit the subject’s data members', 'The subject holds their references and calls each one’s update method when its state changes', 'They each rewrite the subject’s state directly'], 2,
      'The subject keeps a list of registered observers and calls their update method on change; observers neither poll nor inherit.', GOF),
    imq('applying', 'observer-select', 'moderate',
      'When a sensor reading updates, a live graph, an audit log, and a threshold alarm must all react immediately, the sensor must not know their concrete types, and new reactors may be added later. Which approach fits?',
      ['Route every reaction through a central coordinator that decides who reacts to whom', 'Have the sensor maintain a list of dependents and notify each when its reading changes', 'Pass the reading through a series of handlers until one consumes it', 'Wrap the reading update as an undoable command object'], 1,
      'Loosely-coupled, extensible one-to-many notification triggered by the source is Observer. The distractors describe Mediator, Chain of Responsibility, and Command.', GOF),
    imq('analyzing', 'observer-identify', 'difficult',
      'Study this code:\n\nstruct Watcher { virtual void onChange(double temp) = 0; };\nclass Thermostat {\n  std::vector<Watcher*> watchers;\n  double temp = 0;\npublic:\n  void subscribe(Watcher* w) { watchers.push_back(w); }\n  void setTemp(double t) { temp = t; for (auto* w : watchers) w->onChange(temp); }\n};\n\nWhich pattern describes how Thermostat collaborates with its Watchers?',
      ['Mediator — a hub coordinates mutual interaction', 'Visitor — operations are added via double dispatch', 'Observer — the subject broadcasts state changes to registered dependents', 'Iterator — it sequentially accesses elements'], 2,
      'Thermostat keeps a list of dependents and broadcasts each state change to all of them — the one-to-many notification of Observer.', GOF),
    imq('evaluating', 'observer-evaluate', 'difficult',
      'Each dependent registers with a single source and is notified when that source changes; the dependents never coordinate one another’s mutual behaviour through a hub. Should this be classified as Observer or Mediator?',
      ['Mediator — any notification mechanism is Mediator', 'Command — it encapsulates requests as objects', 'State — the source changes behaviour with its state', 'Observer — it is one-way change notification from one source to many dependents, not centralised peer coordination'], 3,
      'One-way change notification from one subject to many dependents is Observer; Mediator would centralise two-way coordination among peers.', SRC),
  ],

  // ================================ Iterator ================================
  'behavioural-iterator': [
    imq('remembering', 'iterator-intent', 'easy',
      'The Iterator pattern lets a client ___.',
      ['walk a collection’s elements one by one without seeing how the collection stores them', 'centralise communication between objects', 'restore an object to a previous state', 'wrap an action so it can be undone'], 0,
      'Iterator provides sequential access while hiding the collection’s representation. The distractors describe Mediator, Memento, and Command.', SRC),
    imq('understanding', 'iterator-rationale', 'moderate',
      'What is the benefit of moving the traversal cursor into a separate iterator object instead of exposing the collection’s internal array?',
      ['It forces the collection to become a single shared instance', 'The collection can keep its storage private and even change it, while clients still traverse uniformly', 'It converts each element to a new type during the walk', 'It lets the collection notify dependents on every step'], 1,
      'A separate cursor decouples traversal from representation, so the collection hides and can later change its internals while clients traverse uniformly.', GOF),
    imq('applying', 'iterator-select', 'moderate',
      'A library exposes a ring buffer and a skip list; users should loop over either with identical code and even run two independent passes over the same container at once, without knowing the storage. Which approach fits?',
      ['Give each container a hub that coordinates who reads next', 'Snapshot the container before each read so it can be rolled back', 'Hand out cursor objects that track position and expose a uniform advance/done interface', 'Add a visitor that defines a new operation per element type'], 2,
      'Uniform sequential access via position-tracking cursors that hide storage and allow concurrent passes is Iterator. The distractors describe Mediator, Memento, and Visitor.', GOF),
    imq('analyzing', 'iterator-identify', 'difficult',
      'Study this code:\n\nclass RangeCursor {\n  int current;\n  int stop;\npublic:\n  RangeCursor(int begin, int end) : current(begin), stop(end) {}\n  bool done() const { return current >= stop; }\n  int value() const { return current; }\n  void advance() { ++current; }\n};\n\nWhich pattern does RangeCursor embody?',
      ['Strategy — an interchangeable algorithm', 'State — behaviour driven by internal mode', 'Command — a reified request', 'Iterator — an object that tracks position and exposes done/value/advance'], 3,
      'An object that holds the traversal position and exposes done/value/advance, hiding the underlying sequence, is an Iterator.', GOF),
    imq('evaluating', 'iterator-evaluate', 'difficult',
      'A so-called traversal API requires callers to read the collection’s public head pointer and node links directly, chasing them manually to loop. Does this honour the Iterator pattern’s intent?',
      ['No — exposing the internal representation to callers defeats the encapsulation Iterator exists to provide', 'Yes — manual pointer chasing is the canonical Iterator', 'Yes — any loop over elements counts as Iterator', 'No — Iterator prohibits sequential access entirely'], 0,
      'Iterator exists precisely to hide the representation; forcing callers to use internal links breaks the encapsulation it is meant to preserve.', SRC),
  ],

  // ================================ Command ================================
  'behavioural-command': [
    imq('remembering', 'command-intent', 'easy',
      'The Command pattern turns ___.',
      ['a collection into a sequentially accessible sequence', 'a request into an object so it can be stored, queued, replayed, or undone', 'a grammar rule into an evaluable class', 'an object’s behaviour into a function of its internal state'], 1,
      'Command reifies a request as an object enabling storage, queuing, replay, and undo. The distractors describe Iterator, Interpreter, and State.', SRC),
    imq('understanding', 'command-enable', 'moderate',
      'What does packaging an action as a Command object let the invoker do that calling the receiver directly does not?',
      ['Compile the program faster', 'Convert the receiver’s interface to another', 'Store, queue, replay, and reverse the action while staying unaware of the concrete receiver', 'Notify a list of dependents automatically'], 2,
      'A reified request can be stored, queued, replayed, and undone, and it decouples the invoker from the concrete receiver and operation.', GOF),
    imq('applying', 'command-select', 'moderate',
      'A graphics editor must record each user action so the program can replay a recorded macro, queue scheduled actions, and reverse the most recent one. Which approach fits?',
      ['Wrap each action in an object exposing execute and undo, and keep them in a history list', 'Delegate each action to a strategy chosen by the user', 'Pass each action through a chain of handlers', 'Snapshot the whole document after every action'], 0,
      'Reifying each action as an execute/undo object held in a history is Command. The distractors describe Strategy, Chain of Responsibility, and Memento.', GOF),
    imq('analyzing', 'command-identify', 'difficult',
      'Study this code:\n\nstruct Action { virtual void run() = 0; virtual void revert() = 0; };\nstruct AddRow : Action {\n  Table& t;\n  Row r;\n  void run() override { t.insert(r); }\n  void revert() override { t.remove(r); }\n};\nclass Runner {\n  std::vector<Action*> log;\npublic:\n  void perform(Action* a) { a->run(); log.push_back(a); }\n};\n\nWhich pattern is shown?',
      ['Memento — it snapshots state for restore', 'State — behaviour changes with internal mode', 'Strategy — an interchangeable algorithm', 'Command — requests reified with run/revert and recorded by an invoker'], 3,
      'Reified requests carrying run/revert, performed and recorded by an invoker that never names the receiver, is the Command pattern.', GOF),
    imq('evaluating', 'command-evaluate', 'difficult',
      'A UI handler calls receiver methods directly inline and claims to support undo by re-reading the current widget values afterwards; there are no request objects. Does this implement Command?',
      ['No — Command requires each request to be a first-class object with execute/undo; direct inline calls are not Command', 'Yes — any feature offering undo is Command', 'Yes — calling the receiver directly is the standard Command form', 'No — Command forbids having any invoker at all'], 0,
      'Command’s essence is making each request a first-class object; calling the receiver directly without command objects is not the pattern.', SRC),
  ],

  // ============================ Template Method ============================
  'behavioural-template-method': [
    imq('remembering', 'template-intent', 'easy',
      'The Template Method pattern keeps an algorithm’s ___ fixed in a base method while letting subclasses supply certain steps.',
      ['list of observers', 'overall step sequence', 'state-transition table', 'request history'], 1,
      'Template Method fixes the overall step sequence in the base class and defers chosen steps to subclasses. The distractors belong to Observer, State, and Command.', SRC),
    imq('understanding', 'template-fixedvary', 'moderate',
      'In a Template Method design, what is fixed and what varies?',
      ['Everything is overridden by each subclass', 'The base method fixes the order of steps while subclasses override only the designated variable steps', 'Nothing in the algorithm can ever be overridden', 'The caller injects the entire algorithm as a separate object'], 1,
      'The invariant control flow lives in the base method; subclasses override only the marked variable steps, not the sequence.', GOF),
    imq('applying', 'template-select', 'moderate',
      'Three exporters share the flow open → gather → render → close and differ only in how they render, and you want that flow written exactly once. Which approach fits?',
      ['Inject the whole export algorithm as a swappable object the caller chooses', 'Put the fixed flow in a base method that calls an overridable render step subclasses implement', 'Pass the export request through a chain of handlers', 'Coordinate the exporters through a central hub'], 1,
      'A fixed skeleton in a base method calling an overridable step per subclass is Template Method; injecting the whole algorithm would be Strategy.', GOF),
    imq('analyzing', 'template-identify', 'difficult',
      'Study this code:\n\nclass Report {\npublic:\n  void build() { header(); body(); footer(); }\nprotected:\n  void header() { /* fixed */ }\n  void footer() { /* fixed */ }\n  virtual void body() = 0;\n};\nclass SalesReport : public Report {\n  void body() override { /* sales rows */ }\n};\n\nWhich pattern does build() demonstrate?',
      ['Strategy — the body is an injected algorithm object', 'State — build changes with internal mode', 'Template Method — a fixed sequence calling an overridable hook step', 'Command — build reifies a request'], 2,
      'A non-overridden base method fixing the order and calling an overridable hook (body) implemented by subclasses is Template Method.', GOF),
    imq('evaluating', 'template-evaluate', 'difficult',
      'An algorithm’s invariant steps live in a base method that invokes overridable hooks, and subclasses customise behaviour only by overriding those hooks via inheritance. Is this better called Template Method or Strategy?',
      ['Strategy — any variation in behaviour is Strategy', 'Observer — the hooks notify dependents', 'Composite — the steps form a tree', 'Template Method — variation comes from subclass overrides of fixed hook steps, not a composed swappable object'], 3,
      'Template Method varies behaviour through inheritance and subclass overrides of fixed hooks; Strategy varies it by composing a swappable algorithm object.', SRC),
  ],

  // ================================ State ================================
  'behavioural-state': [
    imq('remembering', 'state-intent', 'easy',
      'The State pattern lets an object ___.',
      ['change its behaviour when its internal state changes, as though its class changed', 'broadcast every change to a list of dependents', 'capture and later restore its state', 'pass a request along a chain of handlers'], 0,
      'State lets an object alter its behaviour as its internal state changes. The distractors describe Observer, Memento, and Chain of Responsibility.', SRC),
    imq('understanding', 'state-mechanism', 'moderate',
      'How does the State pattern replace a sprawling switch on a status field?',
      ['By making the object a single shared instance', 'By copying the object once per status value', 'By representing each state as an object the host delegates to, with states able to switch the host to another state', 'By notifying observers whenever the status changes'], 2,
      'Each state becomes its own object encapsulating that mode’s behaviour and transitions; the host delegates to the current one, which can switch it.', GOF),
    imq('applying', 'state-select', 'moderate',
      'A vending machine behaves differently when idle, coin-inserted, or dispensing, and each event can move it into another mode with its own allowed actions. Which approach fits?',
      ['Let the caller pick the behaviour from a menu and inject it once', 'Give each mode its own object the machine delegates to, with each mode triggering the move to the next', 'Notify a list of subscribers each time the machine changes', 'Wrap each machine event as an undoable command'], 1,
      'Mode-dependent behaviour where the mode objects themselves drive transitions is State. The distractors describe Strategy, Observer, and Command.', GOF),
    imq('analyzing', 'state-identify', 'difficult',
      'Study this code:\n\nclass Door;\nstruct DoorState { virtual void push(Door& d) = 0; };\nstruct Closed : DoorState { void push(Door& d) override; };\nclass Door {\n  DoorState* state;\npublic:\n  void setState(DoorState* s) { state = s; }\n  void push() { state->push(*this); }\n};\n// Closed::push sets the door to an Open state\n\nWhich pattern does Door’s collaboration with DoorState demonstrate?',
      ['Strategy — an injected algorithm chosen by the caller', 'Observer — it notifies dependents', 'Command — a reified request', 'State — behaviour is delegated to a current-state object that performs the transition'], 3,
      'The host delegates to a current-state object that itself triggers the move to the next state — the hallmark of State, not caller-chosen Strategy.', GOF),
    imq('evaluating', 'state-evaluate', 'difficult',
      'Behaviour is delegated to a polymorphic object, and that object itself decides, based on incoming events, to replace the host’s current behaviour object — driving transitions. Is this State or Strategy?',
      ['State — the behaviour objects drive transitions based on the host’s evolving state', 'Strategy — any delegation to a polymorphic object is Strategy', 'Observer — it notifies dependents of changes', 'Template Method — it fixes an algorithm skeleton'], 0,
      'When the delegated objects themselves drive transitions based on the host’s changing state, it is State; Strategy is caller-chosen and does not self-transition.', SRC),
  ],

  // ======================= Chain of Responsibility =======================
  'behavioural-chain-of-responsibility': [
    imq('remembering', 'chain-intent', 'easy',
      'In the Chain of Responsibility pattern, a request ___.',
      ['is snapshotted so it can be restored later', 'travels along a series of handlers until one of them handles it', 'is broadcast to every registered dependent at once', 'is evaluated as a tree of grammar rules'], 1,
      'Chain of Responsibility forwards a request through handlers until one handles it, decoupling sender from receiver. The distractors describe Memento, Observer, and Interpreter.', SRC),
    imq('understanding', 'chain-decision', 'moderate',
      'What decision does each handler in the chain make when it receives a request?',
      ['Which dependents to notify', 'How to convert the request’s interface', 'Whether to handle the request itself or pass it on to its successor', 'Which single shared instance to return'], 2,
      'Each handler either handles the request or forwards it to the next handler in the chain.', GOF),
    imq('applying', 'chain-select', 'moderate',
      'An expense claim should be tried by a team lead, then a department head, then finance, each either approving within its limit or passing it on, with the submitter unaware of who ultimately approves. Which approach fits?',
      ['Coordinate the approvers through a central hub that routes the claim', 'Link the approvers so each handles the claim within its limit or forwards it to the next', 'Notify all approvers simultaneously and let each react', 'Snapshot the claim before each approval step'], 1,
      'Linked handlers that each handle-or-forward while hiding the receiver from the sender is Chain of Responsibility. The distractors describe Mediator, Observer, and Memento.', GOF),
    imq('analyzing', 'chain-identify', 'difficult',
      'Study this code:\n\nclass Filter {\nprotected:\n  Filter* next = nullptr;\npublic:\n  void chain(Filter* n) { next = n; }\n  virtual void process(Packet& p) { if (next) next->process(p); }\n};\nclass DropSpam : public Filter {\n  void process(Packet& p) override {\n    if (p.isSpam()) return;        // stop here\n    else Filter::process(p);       // forward\n  }\n};\n\nWhich pattern is shown?',
      ['Decorator — a wrapper that always augments and delegates', 'Composite — a part-whole tree', 'Observer — a subject notifying dependents', 'Chain of Responsibility — linked handlers that each handle or forward, possibly stopping the request'], 3,
      'Each handler holds a successor and chooses to consume (stop) or forward the request — Chain of Responsibility, distinct from a Decorator that always delegates.', GOF),
    imq('evaluating', 'chain-evaluate', 'difficult',
      'Each link holds a reference to the next and either stops the request on a condition or calls the next, and the caller never learns which link acted. A teammate calls this Decorator. Is that defensible?',
      ['No — a link that may stop the request and hides the receiver from the sender is Chain of Responsibility, not a Decorator that always delegates while adding behaviour', 'Yes — any chain of wrappers holding a next reference is Decorator', 'Yes — because both forward to a successor', 'No — but only because Decorator requires inheritance'], 0,
      'Conditional handle-or-stop with sender/receiver decoupling is Chain of Responsibility; a Decorator always delegates while augmenting, so the label is wrong.', SRC),
  ],

  // ================================ Mediator ================================
  'behavioural-mediator': [
    imq('remembering', 'mediator-intent', 'easy',
      'According to this module, the role of a Mediator object is to ___.',
      ['evaluates sentences of a small grammar', 'centralises and coordinates communication among a set of objects so they need not reference each other directly', 'gives sequential access to a collection', 'captures an object’s state for restore'], 1,
      'Mediator centralises inter-object communication so colleagues stay decoupled. The distractors describe Interpreter, Iterator, and Memento.', SRC),
    imq('understanding', 'mediator-coupling', 'moderate',
      'How does a Mediator reduce coupling among its colleague objects?',
      ['By merging all colleagues into a single class', 'By converting each colleague’s interface to a common one', 'Colleagues talk only to the mediator, which routes interactions, instead of holding references to one another', 'By making each colleague a single shared instance'], 2,
      'Colleagues communicate through the mediator rather than referencing each other, replacing tangled many-to-many links with one hub.', GOF),
    imq('applying', 'mediator-select', 'moderate',
      'In a booking form, selecting a country must refilter the city list, toggle a postcode field, and enable the submit button; you want to avoid each control holding references to all the others. Which approach fits?',
      ['Route every control’s events through one coordinator that decides how the others react', 'Have each control notify a list of dependents and update themselves', 'Pass each event through a chain of handlers until one acts', 'Give each control an interchangeable algorithm chosen at runtime'], 0,
      'Centralising the controls’ mutual interactions in one coordinator is Mediator; one-way notification would be Observer.', GOF),
    imq('analyzing', 'mediator-identify', 'difficult',
      'Study this code:\n\nclass Tower;\nclass Plane {\n  Tower* tower;\npublic:\n  void requestLanding() { /* tower->coordinate(this) */ }\n};\nclass Tower {\n  std::vector<Plane*> planes;\npublic:\n  void coordinate(Plane* requester) {\n    /* decide order, tell other planes to hold */\n  }\n};\n\nPlanes interact only through Tower. Which pattern is shown?',
      ['Observer — a subject broadcasting to dependents', 'Mediator — a hub coordinating the mutual interaction of otherwise-decoupled peers', 'Facade — a simplified entry point to a subsystem', 'Visitor — operations added by double dispatch'], 1,
      'Peers (planes) that never reference each other and interact only through a central coordinator that governs their mutual behaviour is Mediator.', GOF),
    imq('evaluating', 'mediator-evaluate', 'difficult',
      'A hub receives events from peer objects and instructs other peers how to respond, governing their two-way interaction — not merely forwarding one-way change notifications and not just exposing a simpler entry point to a subsystem. Best classified as?',
      ['Observer — it is one-to-many change notification', 'Facade — it only simplifies access to a subsystem', 'Adapter — it converts incompatible interfaces', 'Mediator — it centralises and coordinates the peers’ mutual communication'], 3,
      'Two-way coordination of peers’ mutual interactions is Mediator; Observer is one-way notification and Facade is one-way subsystem simplification.', SRC),
  ],

  // ================================ Visitor ================================
  'behavioural-visitor': [
    imq('remembering', 'visitor-intent', 'easy',
      'The Visitor pattern lets you ___.',
      ['add new operations over an object structure without modifying the structure’s element classes', 'walk a collection while hiding its storage', 'centralise communication between objects', 'restore an object to a saved state'], 0,
      'Visitor adds operations over a structure without editing the element classes. The distractors describe Iterator, Mediator, and Memento.', SRC),
    imq('understanding', 'visitor-mechanism', 'moderate',
      'What technique lets the correct operation run for each concrete element type in a Visitor design?',
      ['A global switch over an integer type tag', 'Reflection performed at link time', 'Double dispatch — each element’s accept calls back the visitor overload matching the element’s concrete type', 'Copying each element before operating on it'], 2,
      'Double dispatch (element.accept → the visitor’s type-specific visit) selects the right operation for each concrete element type; a new operation is just a new visitor.', GOF),
    imq('applying', 'visitor-select', 'moderate',
      'A document model has paragraph, image, and table nodes; you must keep adding operations like word-count, export-to-HTML, and spell-check without editing each node class for every new operation. Which approach fits?',
      ['Give each node a current-state object that changes its behaviour', 'Move each operation into a separate visitor with one visit method per node type, with nodes calling back via accept', 'Pass each node through a chain of handlers', 'Coordinate the nodes through a central hub'], 1,
      'Adding many operations over a stable element hierarchy without editing the elements, via accept/visit double dispatch, is Visitor. The distractors describe State, Chain of Responsibility, and Mediator.', GOF),
    imq('analyzing', 'visitor-identify', 'difficult',
      'Study this code:\n\nstruct Circle; struct Rect;\nstruct ShapeOp {\n  virtual void on(Circle&) = 0;\n  virtual void on(Rect&) = 0;\n};\nstruct Shape { virtual void accept(ShapeOp&) = 0; };\nstruct Circle : Shape { void accept(ShapeOp& op) override { op.on(*this); } };\n\nWhich pattern does the accept/on collaboration demonstrate?',
      ['Composite — a part-whole tree', 'Mediator — peers coordinated by a hub', 'Observer — a subject notifying dependents', 'Visitor — accept/visit double dispatch adding operations over an element hierarchy'], 3,
      'Elements exposing accept and calling back a type-specific operation (double dispatch) so operations live in the operation object is Visitor.', GOF),
    imq('evaluating', 'visitor-evaluate', 'difficult',
      'A team must add many unrelated operations over a node hierarchy whose set of node types almost never changes. Is Visitor an appropriate choice, and why?',
      ['Yes — Visitor suits a stable element set with frequently-added operations, isolating each operation in its own visitor (accepting that new element types would be costly)', 'No — Visitor is meant for adding new element types cheaply', 'No — Visitor can express only a single operation', 'Yes — because Visitor makes adding new node types trivial'], 0,
      'Visitor fits stable element hierarchies with many added operations; its known cost is that introducing a new element type forces edits to every visitor.', SRC),
  ],

  // ================================ Interpreter ================================
  'behavioural-interpreter': [
    imq('remembering', 'interpreter-intent', 'easy',
      'The Interpreter pattern represents a small language by ___.',
      ['snapshotting the parser’s state for undo', 'defining a class for each grammar rule and using them to evaluate sentences', 'broadcasting each token to a list of observers', 'wrapping each evaluation as an undoable command'], 1,
      'Interpreter models each grammar rule as a class and evaluates sentences built from them. The distractors describe Memento, Observer, and Command.', SRC),
    imq('understanding', 'interpreter-represent', 'moderate',
      'How is a sentence typically represented and evaluated in an Interpreter design?',
      ['As a single regular expression matched once', 'As one large switch statement over the raw text', 'As a tree of rule objects, with evaluation recursing over the tree by calling each node’s interpret', 'As a flat array of characters scanned left to right only'], 2,
      'A sentence becomes an abstract syntax tree of rule objects, and interpretation recurses over the tree, each node interpreting its children.', GOF),
    imq('applying', 'interpreter-select', 'moderate',
      'Users type small filter expressions such as price > 10 AND inStock that nest arbitrarily, and you must evaluate them by composing rules recursively. Which approach fits?',
      ['Model each operator and operand as a class with an interpret method and compose them into a tree', 'Add a new visitor for each kind of filter without changing the operand classes', 'Pick the evaluation algorithm from a menu at runtime', 'Pass the expression through a chain of handlers until one evaluates it'], 0,
      'Representing each grammar element as a composable evaluable node and recursing is Interpreter. The distractors describe Visitor, Strategy, and Chain of Responsibility.', GOF),
    imq('analyzing', 'interpreter-identify', 'difficult',
      'Study this code:\n\nstruct Term { virtual int eval(const Env& e) const = 0; };\nstruct Const : Term { int n; int eval(const Env&) const override { return n; } };\nstruct Mul : Term {\n  const Term* l; const Term* r;\n  int eval(const Env& e) const override { return l->eval(e) * r->eval(e); }\n};\n\nWhich pattern is shown?',
      ['Composite — a structural part-whole tree with no evaluation semantics', 'Strategy — an interchangeable algorithm', 'Observer — a subject notifying dependents', 'Interpreter — grammar-rule classes with eval composed into an expression tree'], 3,
      'Grammar-rule classes carrying eval and composed into an expression tree that evaluates a sentence is the Interpreter pattern.', GOF),
    imq('evaluating', 'interpreter-evaluate', 'difficult',
      'A team adopts the Interpreter pattern to implement a large, performance-critical general-purpose programming language with deeply nested grammar. Is this an appropriate use, and why?',
      ['No — Interpreter suits small, stable grammars; a large, complex, performance-critical language is better served by parser/compiler tooling', 'Yes — Interpreter scales best to large languages', 'Yes — raw evaluation speed is Interpreter’s main strength', 'No — Interpreter is unable to evaluate any expression'], 0,
      'Interpreter fits small, stable grammars; for large complex performance-critical languages the class-per-rule tree becomes unwieldy and a parser/compiler is preferable.', SRC),
  ],

  // ================================ Memento ================================
  'behavioural-memento': [
    imq('remembering', 'memento-intent', 'easy',
      'The Memento pattern is used to ___.',
      ['coordinate communication among objects through a hub', 'select an algorithm at runtime', 'capture an object’s state so it can be restored later without exposing its internals', 'pass a request along a chain of handlers'], 2,
      'Memento captures and restores state without breaking encapsulation. The distractors describe Mediator, Strategy, and Chain of Responsibility.', SRC),
    imq('understanding', 'memento-encaps', 'moderate',
      'How does the Memento pattern preserve encapsulation while saving an object’s state?',
      ['It makes all of the object’s fields public so anyone can copy them', 'The originator produces an opaque memento that the caretaker stores without reading its contents', 'It exposes the state through getters available to every caller', 'It copies the originator’s class definition at runtime'], 1,
      'The originator builds the memento and the caretaker holds it opaquely, never inspecting the internals, so encapsulation is preserved.', GOF),
    imq('applying', 'memento-select', 'moderate',
      'A text editor must support unlimited undo by saving the buffer’s state at each step and restoring an earlier one on demand, without exposing the buffer’s private fields. Which approach fits?',
      ['Have the buffer return an opaque snapshot a history keeper stores and hands back to restore it', 'Wrap each edit as a command and replay it in reverse', 'Give the buffer a current-state object that changes its behaviour', 'Notify a list of dependents whenever the buffer changes'], 0,
      'Producing an opaque snapshot stored by a caretaker and used to restore the object, without exposing internals, is Memento. The distractors lean toward Command, State, and Observer.', GOF),
    imq('analyzing', 'memento-identify', 'difficult',
      'Study this code:\n\nclass Canvas {\n  std::vector<Stroke> strokes;\npublic:\n  class Snapshot {\n    friend class Canvas;\n    std::vector<Stroke> saved;\n    explicit Snapshot(std::vector<Stroke> s) : saved(std::move(s)) {}\n  };\n  Snapshot capture() const { return Snapshot(strokes); }\n  void restore(const Snapshot& s) { strokes = s.saved; }\n};\n\nWhich pattern is shown?',
      ['Command — a reified request with execute/undo', 'State — behaviour changes with internal mode', 'Prototype — cloning via a virtual copy', 'Memento — an originator producing/consuming an opaque state snapshot only it can read'], 3,
      'An originator emitting an opaque snapshot (whose internals only it can read via friendship) and restoring from it is the Memento pattern.', GOF),
    imq('evaluating', 'memento-evaluate', 'difficult',
      'A so-called snapshot design makes every internal field of an object public so external code can read them all, copy them, and later write them back directly. Does this honour Memento’s intent?',
      ['No — Memento captures state without breaking encapsulation; exposing all internals publicly violates that core intent', 'Yes — public fields are a requirement of Memento', 'Yes — any save-and-restore scheme is Memento', 'No — because Memento is unable to restore state at all'], 0,
      'Memento exists to capture state while keeping internals hidden; making every field public defeats the encapsulation the pattern is meant to protect.', SRC),
  ],
};
