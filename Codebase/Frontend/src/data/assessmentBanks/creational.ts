// Authored Creational formal Form A/B objective items (5 per form: Remember,
// Understand, Apply, Analyze, Evaluate). Create stays in each module's practical
// C++ task (module.practicalExam) — never an MCQ. Apply/Analyze/Evaluate
// scenarios do not name the pattern in the stem. All items validationStatus:
// 'draft' (not expert-validated). Builder is authored elsewhere and untouched.
// Correct-option positions are deliberately spread across A/B/C/D to avoid
// answer-position bias.

import type { ObjectiveAssessmentQuestion } from '../learningModules';

type Form = ReadonlyArray<ObjectiveAssessmentQuestion>;

const GOF = 'Gamma, Helm, Johnson, Vlissides (1994), Design Patterns';
const NESTERUK = 'Nesteruk (2022), Design Patterns in Modern C++';
const MODULE = 'CodiNeo Creational module content';

const mk = (
  id: string, paired: string,
  taxonomy: 'remembering' | 'understanding' | 'applying' | 'analyzing' | 'evaluating',
  bloomLevel: 'remember' | 'understand' | 'apply' | 'analyze' | 'evaluate',
  competencyId: string, difficulty: 'easy' | 'moderate' | 'difficult',
  question: string, options: [string, string, string, string], correctIndex: 0 | 1 | 2 | 3,
  rationale: string, sources: ReadonlyArray<string>,
): ObjectiveAssessmentQuestion => ({
  id, form: id.includes(':A') ? 'A' : 'B', pairedQuestionId: paired, type: 'mcq', taxonomy, bloomLevel, competencyId, difficulty,
  question, options, correctIndex, rationale, sourceReferences: sources, validationStatus: 'draft',
});

// ============================ Singleton ============================
const singletonA: Form = [
  mk('creational-singleton:A1', 'creational-singleton:B1', 'remembering', 'remember', 'singleton-intent', 'easy',
    'What does the Singleton pattern guarantee?',
    ['A class has exactly one instance with a single global access point', 'A class can be freely subclassed and copied', 'Objects are produced in matching families', 'Objects are built through ordered steps'], 0,
    'Singleton ensures a class has exactly one instance and provides one global access point to it. The other options describe subclassing, Abstract Factory, and Builder.', [GOF, MODULE]),
  mk('creational-singleton:A2', 'creational-singleton:B2', 'understanding', 'understand', 'singleton-enforce', 'moderate',
    'How is the single-instance guarantee typically enforced in C++?',
    ['By marking every method const', 'By making the class abstract', 'By a non-public constructor plus a static accessor that returns the sole instance', 'By deleting the destructor'], 2,
    'A hidden (non-public) constructor prevents outside construction, and a static accessor returns the one instance. The other options do not constrain instance count.', [NESTERUK, MODULE]),
  mk('creational-singleton:A3', 'creational-singleton:B3', 'applying', 'apply', 'singleton-select', 'moderate',
    'A system needs exactly one shared configuration object, created once and reachable everywhere. Which pattern fits?',
    ['Prototype', 'Factory Method', 'Abstract Factory', 'Singleton'], 3,
    'One shared instance with a global access point is the Singleton use case; the distractors address copying, deferred subclass choice, and product families.', [GOF]),
  mk('creational-singleton:A4', 'creational-singleton:B4', 'analyzing', 'analyze', 'singleton-identify', 'difficult',
    'Examine:  class Cfg { Cfg() = default; public: static Cfg& instance() { static Cfg c; return c; } };  Which pattern does this indicate?',
    ['Singleton', 'Factory Method', 'Builder', 'Prototype'], 0,
    'A non-public constructor plus a static accessor returning one function-local static is the (Meyers) Singleton idiom; nothing here creates families, products, or copies.', [NESTERUK]),
  mk('creational-singleton:A5', 'creational-singleton:B5', 'evaluating', 'evaluate', 'singleton-evaluate', 'difficult',
    'A class exposes a public static getInstance() but also leaves its constructor public. Does this correctly implement Singleton, and why?',
    ['Yes — the static getter is sufficient', 'Yes — provided the class is marked final', 'No — Singletons must allocate on the heap', 'No — a public constructor lets callers create extra instances, breaking the single-instance guarantee'], 3,
    'The single-instance invariant requires preventing outside construction; a public constructor allows additional instances, so the implementation is incorrect regardless of the getter.', [GOF, NESTERUK]),
];
const singletonB: Form = [
  mk('creational-singleton:B1', 'creational-singleton:A1', 'remembering', 'remember', 'singleton-intent', 'easy',
    'The Singleton pattern primarily ensures that ___.',
    ['objects are cloned from a prototype', 'related product families are produced together', 'one instance is reachable from a single global access point', 'an object is assembled step by step'], 2,
    'Singleton ensures a single instance with one global access point. The distractors describe Prototype, Abstract Factory, and Builder.', [GOF, MODULE]),
  mk('creational-singleton:B2', 'creational-singleton:A2', 'understanding', 'understand', 'singleton-enforce', 'moderate',
    'Which member combination is characteristic of a C++ Singleton?',
    ['A public constructor and a virtual destructor', 'Several overloaded public constructors', 'A public clone() method', 'A hidden constructor and a static method returning the sole instance'], 3,
    'A hidden constructor blocks external construction while a static accessor returns the one instance. Public/overloaded constructors permit many instances; clone() is Prototype.', [NESTERUK, MODULE]),
  mk('creational-singleton:B3', 'creational-singleton:A3', 'applying', 'apply', 'singleton-select', 'moderate',
    'An application must route all logging through one logger object, created on first use and shared app-wide. Which pattern best fits?',
    ['Builder', 'Singleton', 'Prototype', 'Adapter'], 1,
    'A single shared, lazily-created object with global access is Singleton; the distractors solve construction, copying, and interface conversion.', [GOF]),
  mk('creational-singleton:B4', 'creational-singleton:A4', 'analyzing', 'analyze', 'singleton-identify', 'difficult',
    'Examine:  // ctor is private\n  Logger& logger() { static Logger inst; return inst; }  Which pattern is this idiom?',
    ['Singleton', 'Prototype', 'Abstract Factory', 'Method Chaining'], 0,
    'A private constructor plus a function-local static returned by reference is the lazy (Meyers) Singleton; it is not copying, family creation, or fluent chaining.', [NESTERUK]),
  mk('creational-singleton:B5', 'creational-singleton:A5', 'evaluating', 'evaluate', 'singleton-evaluate', 'difficult',
    'A developer keeps the instance in a global pointer and calls new in several places “just in case.” Is this a sound Singleton?',
    ['Yes — a global pointer guarantees uniqueness', 'Yes — provided it is wrapped in a namespace', 'No — multiple new calls create multiple instances, violating the single-instance invariant', 'No — Singletons cannot use pointers at all'], 2,
    'Calling new in several places creates several instances, breaking uniqueness; the access mechanism (global pointer/namespace) does not enforce a single instance.', [NESTERUK]),
];

// ============================ Factory Method ============================
const factoryA: Form = [
  mk('creational-factory-method:A1', 'creational-factory-method:B1', 'remembering', 'remember', 'fm-intent', 'easy',
    'What is the intent of the Factory Method pattern?',
    ['Define an interface for creating an object but let subclasses decide which concrete class to instantiate', 'Ensure a class has a single instance', 'Copy an existing object', 'Wrap an incompatible interface'], 0,
    'Factory Method defers the choice of concrete product to subclasses via an overridable creation method. The distractors describe Singleton, Prototype, and Adapter.', [GOF, MODULE]),
  mk('creational-factory-method:A2', 'creational-factory-method:B2', 'understanding', 'understand', 'fm-understand', 'moderate',
    'In Factory Method, who decides which concrete product is created?',
    ['The client’s calling code directly', 'The compiler', 'A subclass that overrides the creation method', 'A global registry only'], 2,
    'The creator declares a creation method; a subclass overrides it to choose the concrete product, decoupling clients from concretes.', [GOF]),
  mk('creational-factory-method:A3', 'creational-factory-method:B3', 'applying', 'apply', 'fm-select', 'moderate',
    'A framework defines a workflow but must let each plug-in decide which concrete document object the workflow creates. Which pattern fits?',
    ['Singleton', 'Prototype', 'Method Chaining', 'Factory Method'], 3,
    'Letting subclasses/plug-ins choose the concrete product while the base defines the workflow is Factory Method; the distractors solve other problems.', [GOF]),
  mk('creational-factory-method:A4', 'creational-factory-method:B4', 'analyzing', 'analyze', 'fm-identify', 'difficult',
    'Examine:  struct Creator { virtual Product* create() = 0; void run(){ auto* p = create(); /* use p */ } };  struct PdfCreator : Creator { Product* create() override { return new Pdf(); } };  Which pattern is this?',
    ['Factory Method', 'Abstract Factory', 'Builder', 'Singleton'], 0,
    'A base class with an overridable create() that subclasses implement to pick the concrete product is Factory Method (one product per call), not a family factory or step builder.', [GOF, NESTERUK]),
  mk('creational-factory-method:A5', 'creational-factory-method:B5', 'evaluating', 'evaluate', 'fm-evaluate', 'difficult',
    'A class named ShapeFactory contains one giant switch on a string to new each shape, with no polymorphic creator. Is this Factory Method?',
    ['Yes — the name ShapeFactory makes it Factory Method', 'Yes — any creation code is Factory Method', 'No — Factory Method requires templates', 'No — the name does not matter; without a polymorphic creation method overridden by subclasses it is a simple/switch factory, not Factory Method'], 3,
    'Factory Method’s defining structure is a creation method overridden by subclasses; a string switch is a simple factory. The class name is not evidence of the pattern.', [GOF, NESTERUK]),
];
const factoryB: Form = [
  mk('creational-factory-method:B1', 'creational-factory-method:A1', 'remembering', 'remember', 'fm-intent', 'easy',
    'Factory Method is best described as a pattern that ___.',
    ['guarantees a single shared instance', 'produces whole families of related products', 'lets subclasses decide which concrete class an overridable method instantiates', 'adds behaviour around an object transparently'], 2,
    'Factory Method delegates the concrete-product choice to subclasses through an overridable method. The distractors are Singleton, Abstract Factory, and Decorator.', [GOF, MODULE]),
  mk('creational-factory-method:B2', 'creational-factory-method:A2', 'understanding', 'understand', 'fm-understand', 'moderate',
    'How does Factory Method decouple a creator from the concrete products it uses?',
    ['By making the product a global variable', 'By hard-coding the product type in the base class', 'By cloning a prototype instance', 'By calling an overridable creation method, so subclasses supply the concrete type'], 3,
    'The creator calls its own overridable creation method; subclasses decide the concrete type, so the creator never names a concrete product.', [GOF]),
  mk('creational-factory-method:B3', 'creational-factory-method:A3', 'applying', 'apply', 'fm-select', 'moderate',
    'A base dialog class runs the same open/close flow but each dialog kind must instantiate its own button type. Which pattern is most appropriate?',
    ['Singleton', 'Factory Method', 'Adapter', 'Prototype'], 1,
    'A shared flow whose concrete sub-object is chosen by subclasses is Factory Method; the distractors address single-instance, interface conversion, and copying.', [GOF]),
  mk('creational-factory-method:B4', 'creational-factory-method:A4', 'analyzing', 'analyze', 'fm-identify', 'difficult',
    'Examine:  struct ShapeCreator { virtual Shape* make() = 0; };  struct CircleCreator : ShapeCreator { Shape* make() override { return new Circle(); } };  Which pattern is this structural evidence of?',
    ['Factory Method', 'Builder', 'Prototype', 'Abstract Factory'], 0,
    'A virtual make() overridden by a subclass to return one concrete product is Factory Method; it produces a single product, not a family or a step-built object.', [GOF, NESTERUK]),
  mk('creational-factory-method:B5', 'creational-factory-method:A5', 'evaluating', 'evaluate', 'fm-evaluate', 'difficult',
    'A reviewer sees a creator base class with create() overridden by two subclasses to return different products. Is calling this Factory Method defensible?',
    ['No — only a string switch counts as Factory Method', 'No — it must also be a Singleton', 'Yes — an overridable creation method specialised by subclasses to choose the concrete product is exactly Factory Method', 'Yes — but only if the class is named Factory'], 2,
    'Subclasses overriding a creation method to pick the concrete product is the defining structure of Factory Method; the name and Singleton status are irrelevant.', [GOF]),
];

// ============================ Method Chaining ============================
const chainingA: Form = [
  mk('creational-method-chaining:A1', 'creational-method-chaining:B1', 'remembering', 'remember', 'mc-intent', 'easy',
    'What does the Method Chaining idiom let you do?',
    ['Set several options on the same object in one fluent expression', 'Guarantee one instance', 'Create matching product families', 'Convert an incompatible interface'], 0,
    'Method Chaining returns the same object from each setter so calls can be chained fluently. The distractors are Singleton, Abstract Factory, and Adapter.', [NESTERUK, MODULE]),
  mk('creational-method-chaining:A2', 'creational-method-chaining:B2', 'understanding', 'understand', 'mc-understand', 'moderate',
    'What must a setter return to enable method chaining in C++?',
    ['void', 'a new copy of the object', 'a reference (or pointer) to the same object, e.g. return *this', 'a boolean success flag'], 2,
    'Returning *this (a reference to the same object) lets the next call chain on it. Returning void, a copy, or a flag breaks the chain.', [NESTERUK]),
  mk('creational-method-chaining:A3', 'creational-method-chaining:B3', 'applying', 'apply', 'mc-select', 'moderate',
    'A configuration object should be set up in one readable expression like cfg.host("x").port(80).tls(true);. Which technique enables this?',
    ['Returning void from each setter', 'Making the class a Singleton', 'Cloning the object per setter', 'Having each setter return a reference to the object so calls chain'], 3,
    'Each setter returning a self-reference makes the fluent chain possible. The other options prevent chaining or solve unrelated problems.', [NESTERUK]),
  mk('creational-method-chaining:A4', 'creational-method-chaining:B4', 'analyzing', 'analyze', 'mc-identify', 'difficult',
    'Examine:  struct Pizza { Pizza& size(int s){ s_=s; return *this; } Pizza& cheese(bool c){ c_=c; return *this; } };  What idiom does this show?',
    ['Method Chaining (fluent interface)', 'Prototype', 'Singleton', 'Abstract Factory'], 0,
    'Setters that mutate and return *this form a fluent interface (Method Chaining). No copying, single-instance, or family creation is present.', [NESTERUK]),
  mk('creational-method-chaining:A5', 'creational-method-chaining:B5', 'evaluating', 'evaluate', 'mc-evaluate', 'difficult',
    'A “fluent” API returns a brand-new copy of the object from every setter instead of *this. Does this correctly implement method chaining, and what is the effect?',
    ['Yes — it is identical to returning *this', 'Yes — copies are required for chaining', 'No — chaining is impossible in C++', 'No — returning copies means earlier settings may be lost and each call mutates a different object, defeating the fluent intent'], 3,
    'Returning fresh copies means each call operates on a different object, so accumulated configuration can be lost; chaining requires returning the same object (*this).', [NESTERUK]),
];
const chainingB: Form = [
  mk('creational-method-chaining:B1', 'creational-method-chaining:A1', 'remembering', 'remember', 'mc-intent', 'easy',
    'Method Chaining is mainly used to ___.',
    ['ensure a single instance', 'produce related product families', 'configure one object through a fluent sequence of calls', 'wrap an incompatible interface'], 2,
    'Method Chaining configures the same object via a fluent call sequence. The distractors are Singleton, Abstract Factory, and Adapter.', [NESTERUK, MODULE]),
  mk('creational-method-chaining:B2', 'creational-method-chaining:A2', 'understanding', 'understand', 'mc-understand', 'moderate',
    'Why does each chained method return *this?',
    ['To make a defensive copy', 'To enforce a single instance', 'To convert the return type', 'So the following call can operate on the same object, continuing the chain'], 3,
    'Returning *this hands the same object to the next call, sustaining the fluent chain. The other options are unrelated.', [NESTERUK]),
  mk('creational-method-chaining:B3', 'creational-method-chaining:A3', 'applying', 'apply', 'mc-select', 'moderate',
    'A query builder should read like q.select("*").from("t").where("id=1");. What enables this fluent style?',
    ['Each method returns void', 'Each method returns a reference to the builder so calls chain', 'The builder is cloned each call', 'The builder is a Singleton'], 1,
    'Each method returning a reference to the builder enables chaining; void, cloning, or Singleton would not.', [NESTERUK]),
  mk('creational-method-chaining:B4', 'creational-method-chaining:A4', 'analyzing', 'analyze', 'mc-identify', 'difficult',
    'Examine:  Query& Query::where(std::string c){ clauses_.push_back(c); return *this; }  // similar for select(), from()  Which idiom is this?',
    ['Method Chaining (fluent interface)', 'Singleton', 'Prototype', 'Factory Method'], 0,
    'Mutating member functions that return *this form a fluent (method-chaining) interface; there is no single-instance, copy, or factory structure.', [NESTERUK]),
  mk('creational-method-chaining:B5', 'creational-method-chaining:A5', 'evaluating', 'evaluate', 'mc-evaluate', 'difficult',
    'A setter in a “fluent” class returns void but the author still writes a.x().y();. Is the chaining implemented correctly?',
    ['Yes — void return is fine for chaining', 'Yes — provided the methods are const', 'No — with a void return the second call has no object to act on, so it will not compile/chain', 'No — only Singletons can chain'], 2,
    'Chaining needs each call to yield the object; a void return leaves nothing to call the next method on, so the chain cannot work.', [NESTERUK]),
];

// ============================ Prototype ============================
const prototypeA: Form = [
  mk('creational-prototype:A1', 'creational-prototype:B1', 'remembering', 'remember', 'pr-intent', 'easy',
    'What is the intent of the Prototype pattern?',
    ['Create new objects by copying an existing instance (a prototype)', 'Guarantee a single instance', 'Let subclasses choose the concrete product', 'Assemble an object through ordered steps'], 0,
    'Prototype creates new objects by cloning an existing prototypical instance. The distractors are Singleton, Factory Method, and Builder.', [GOF, MODULE]),
  mk('creational-prototype:A2', 'creational-prototype:B2', 'understanding', 'understand', 'pr-understand', 'moderate',
    'Why is Prototype useful when object creation is expensive or configuration-heavy?',
    ['It avoids creating objects at all', 'It copies a ready, pre-configured instance instead of rebuilding from scratch', 'It guarantees thread safety', 'It removes the need for constructors'], 1,
    'Cloning a pre-configured prototype reuses its setup, avoiding costly re-construction. The other options misstate Prototype’s benefit.', [GOF]),
  mk('creational-prototype:A3', 'creational-prototype:B3', 'applying', 'apply', 'pr-select', 'moderate',
    'An editor must duplicate a fully-configured shape (with styling) at runtime without knowing its concrete type. Which pattern fits?',
    ['Singleton', 'Abstract Factory', 'Method Chaining', 'Prototype'], 3,
    'Duplicating a configured object polymorphically (without its concrete type) is Prototype via clone(); the distractors solve other problems.', [GOF]),
  mk('creational-prototype:A4', 'creational-prototype:B4', 'analyzing', 'analyze', 'pr-identify', 'difficult',
    'Examine:  struct Shape { virtual Shape* clone() const = 0; };  struct Circle : Shape { Shape* clone() const override { return new Circle(*this); } };  Which pattern is this?',
    ['Prototype', 'Factory Method', 'Builder', 'Singleton'], 0,
    'A virtual clone() that copy-constructs the same concrete type is the Prototype structure; it copies rather than choosing or assembling a product.', [GOF, NESTERUK]),
  mk('creational-prototype:A5', 'creational-prototype:B5', 'evaluating', 'evaluate', 'pr-evaluate', 'difficult',
    'A clone() returns new Base(*this) for every subclass (no override), so cloning a Derived yields a Base. Is this a correct Prototype, and why?',
    ['Yes — base copying is sufficient', 'Yes — provided Base is abstract', 'No — Prototype forbids inheritance', 'No — without a polymorphic override clone() slices the object, losing the derived type'], 3,
    'Cloning must be polymorphic so each subclass copies its own type; a fixed new Base(*this) slices derived objects, breaking Prototype.', [NESTERUK]),
];
const prototypeB: Form = [
  mk('creational-prototype:B1', 'creational-prototype:A1', 'remembering', 'remember', 'pr-intent', 'easy',
    'The Prototype pattern creates new objects by ___.',
    ['assembling them step by step', 'sharing a single global instance', 'cloning an existing pre-built instance', 'producing related families'], 2,
    'Prototype clones an existing instance to make new ones. The distractors are Builder, Singleton, and Abstract Factory.', [GOF, MODULE]),
  mk('creational-prototype:B2', 'creational-prototype:A2', 'understanding', 'understand', 'pr-understand', 'moderate',
    'What advantage does cloning a prototype give over constructing from scratch?',
    ['It eliminates memory allocation', 'It reuses an already-configured instance, avoiding repeated setup', 'It guarantees a single instance', 'It converts interfaces'], 1,
    'Cloning carries over the prototype’s configuration, saving repeated setup work; the other options are not Prototype benefits.', [GOF]),
  mk('creational-prototype:B3', 'creational-prototype:A3', 'applying', 'apply', 'pr-select', 'moderate',
    'A game spawns many enemies identical to a configured template at runtime, varying only position. Which pattern most directly applies?',
    ['Prototype', 'Singleton', 'Factory Method', 'Adapter'], 0,
    'Spawning copies of a configured template is Prototype (clone then tweak); the distractors do not copy a configured instance.', [GOF]),
  mk('creational-prototype:B4', 'creational-prototype:A4', 'analyzing', 'analyze', 'pr-identify', 'difficult',
    'Examine:  Document* dup = original->clone();  where clone() is virtual and each subclass returns a copy of itself. Which pattern is this evidence of?',
    ['Builder', 'Abstract Factory', 'Prototype', 'Singleton'], 2,
    'A polymorphic clone() that returns a copy of the concrete subclass is the Prototype pattern; no stepwise build or family creation is involved.', [GOF, NESTERUK]),
  mk('creational-prototype:B5', 'creational-prototype:A5', 'evaluating', 'evaluate', 'pr-evaluate', 'difficult',
    'A Prototype holds a raw pointer member and clone() does a shallow copy. What is the risk, and is it a sound clone?',
    ['No risk — shallow copy is always correct', 'Sound only if the class is final', 'Unsound — Prototype cannot hold pointers', 'Risky/unsound — both objects share the same pointed-to resource, so a deep copy is usually needed for an independent clone'], 3,
    'A shallow copy makes clones share the same owned resource, causing aliasing/double-free bugs; an independent clone usually needs a deep copy.', [NESTERUK]),
];

// ============================ Abstract Factory ============================
const abstractFactoryA: Form = [
  mk('creational-abstract-factory:A1', 'creational-abstract-factory:B1', 'remembering', 'remember', 'af-intent', 'easy',
    'What is the intent of the Abstract Factory pattern?',
    ['Provide an interface for creating families of related objects without naming their concrete classes', 'Ensure a single instance', 'Copy an existing object', 'Add behaviour to an object dynamically'], 0,
    'Abstract Factory creates families of related products behind one interface, hiding concrete classes. The distractors are Singleton, Prototype, and Decorator.', [GOF, MODULE]),
  mk('creational-abstract-factory:A2', 'creational-abstract-factory:B2', 'understanding', 'understand', 'af-understand', 'moderate',
    'How does Abstract Factory differ from Factory Method?',
    ['It is identical to Factory Method', 'It creates a single product, while Factory Method creates families', 'It creates whole families of related products, while Factory Method creates one product via subclass override', 'It only applies to Singletons'], 2,
    'Abstract Factory builds families of related products together; Factory Method defers one product’s creation to a subclass. They are not identical.', [GOF]),
  mk('creational-abstract-factory:A3', 'creational-abstract-factory:B3', 'applying', 'apply', 'af-select', 'moderate',
    'A UI toolkit must produce matching widgets (button, checkbox, menu) per theme, guaranteed to belong to the same look-and-feel. Which pattern fits?',
    ['Prototype', 'Singleton', 'Method Chaining', 'Abstract Factory'], 3,
    'Producing a coherent family of related widgets per theme is Abstract Factory; the distractors do not enforce matching families.', [GOF]),
  mk('creational-abstract-factory:A4', 'creational-abstract-factory:B4', 'analyzing', 'analyze', 'af-identify', 'difficult',
    'Examine:  struct GuiFactory { virtual Button* makeButton() = 0; virtual Checkbox* makeCheckbox() = 0; };  Which pattern is this interface part of?',
    ['Factory Method', 'Builder', 'Abstract Factory', 'Prototype'], 2,
    'One factory interface with multiple create methods for related products (Button + Checkbox) is Abstract Factory; a single create method would be Factory Method.', [GOF, NESTERUK]),
  mk('creational-abstract-factory:A5', 'creational-abstract-factory:B5', 'evaluating', 'evaluate', 'af-evaluate', 'difficult',
    'A concrete WinFactory implements makeButton() returning a WinButton but makeCheckbox() returns a MacCheckbox. Does this honor Abstract Factory’s intent?',
    ['Yes — any concrete returns are fine', 'Yes — provided both compile', 'No — Abstract Factory forbids more than one product', 'No — a concrete factory must return products from one consistent family; mixing Win and Mac breaks the family guarantee'], 3,
    'Abstract Factory’s value is that one concrete factory yields a matching family; returning a Mac checkbox from a Windows factory violates that guarantee.', [GOF]),
];
const abstractFactoryB: Form = [
  mk('creational-abstract-factory:B1', 'creational-abstract-factory:A1', 'remembering', 'remember', 'af-intent', 'easy',
    'Abstract Factory is best described as a pattern that ___.',
    ['guarantees one global instance', 'clones a configured instance', 'creates families of related objects behind one interface, hiding concrete types', 'assembles one object via ordered steps'], 2,
    'Abstract Factory produces related product families through one interface. The distractors are Singleton, Prototype, and Builder.', [GOF, MODULE]),
  mk('creational-abstract-factory:B2', 'creational-abstract-factory:A2', 'understanding', 'understand', 'af-understand', 'moderate',
    'Which statement correctly contrasts Abstract Factory with Factory Method?',
    ['Abstract Factory makes one product; Factory Method makes families', 'They are the same pattern', 'Abstract Factory only works with Singletons', 'Abstract Factory creates a family of related products; Factory Method creates a single product via an overridden method'], 3,
    'Abstract Factory creates coherent families; Factory Method creates one product through a subclass override. The other options invert or conflate them.', [GOF]),
  mk('creational-abstract-factory:B3', 'creational-abstract-factory:A3', 'applying', 'apply', 'af-select', 'moderate',
    'A cross-platform app must create matching sets of UI controls for either Windows or macOS, never mixing them. Which pattern is most appropriate?',
    ['Abstract Factory', 'Prototype', 'Singleton', 'Adapter'], 0,
    'Creating a coherent, non-mixed family per platform is Abstract Factory; the distractors do not enforce family coherence.', [GOF]),
  mk('creational-abstract-factory:B4', 'creational-abstract-factory:A4', 'analyzing', 'analyze', 'af-identify', 'difficult',
    'Examine:  struct WinFactory : GuiFactory { Button* makeButton() override { return new WinButton(); } Checkbox* makeCheckbox() override { return new WinCheckbox(); } };  Which pattern does this concrete class participate in?',
    ['Builder', 'Prototype', 'Singleton', 'Abstract Factory'], 3,
    'A concrete factory implementing several related create methods to return one consistent family (WinButton + WinCheckbox) is Abstract Factory.', [GOF, NESTERUK]),
  mk('creational-abstract-factory:B5', 'creational-abstract-factory:A5', 'evaluating', 'evaluate', 'af-evaluate', 'difficult',
    'A team replaces two separate makeButton()/makeCheckbox() factory methods with one makeWidget(string kind) switch returning new-ed widgets. Is this still Abstract Factory?',
    ['Yes — it is unchanged', 'No — collapsing related product methods into a string switch loses the typed family interface that defines Abstract Factory', 'Yes — provided the class is named Factory', 'No — Abstract Factory requires Singletons'], 1,
    'Abstract Factory relies on a typed interface of related create methods producing a coherent family; a single stringly-typed switch discards that structure.', [GOF, NESTERUK]),
];

export const CREATIONAL_ASSESSMENT_FORMS: Record<string, { A: Form; B: Form }> = {
  'creational-singleton': { A: singletonA, B: singletonB },
  'creational-factory-method': { A: factoryA, B: factoryB },
  'creational-method-chaining': { A: chainingA, B: chainingB },
  'creational-prototype': { A: prototypeA, B: prototypeB },
  'creational-abstract-factory': { A: abstractFactoryA, B: abstractFactoryB },
};
