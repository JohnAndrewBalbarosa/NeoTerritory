// Authored Structural formal Form A/B objective items (Remember→Evaluate).
// Create stays in each module's practical C++ task — never an MCQ.
// Repository is a NON-GoF enterprise/application architecture pattern: it is
// assessed conceptually (no practicalExam, no automated detector) and is NOT
// claimed to be a GoF structural pattern. All items validationStatus 'draft'.
// Correct-option positions are spread across A/B/C/D to avoid position bias.

import type { ObjectiveAssessmentQuestion } from '../learningModules';

type Form = ReadonlyArray<ObjectiveAssessmentQuestion>;

const GOF = 'Gamma, Helm, Johnson, Vlissides (1994), Design Patterns';
const MODULE = 'CodiNeo Structural module content';
const REPO = ['CodiNeo Structural module content', 'Fowler (2002), Patterns of Enterprise Application Architecture — Repository', 'Evans (2003), Domain-Driven Design'];

const mk = (
  id: string, paired: string,
  taxonomy: 'remembering' | 'understanding' | 'applying' | 'analyzing' | 'evaluating',
  bloomLevel: 'remember' | 'understand' | 'apply' | 'analyze' | 'evaluate',
  competencyId: string, difficulty: 'easy' | 'moderate' | 'difficult',
  question: string, options: [string, string, string, string], correctIndex: 0 | 1 | 2 | 3,
  rationale: string, sources: ReadonlyArray<string>,
): ObjectiveAssessmentQuestion => ({
  id, form: id.includes(':A') ? 'A' : 'B', pairedQuestionId: paired, type: 'mcq', taxonomy, bloomLevel, competencyId, difficulty,
  question, options, correctIndex, rationale, sourceReferences: sources, validationStatus: 'draft',
});

// ================================ Adapter ================================
const adapterA: Form = [
  mk('structural-adapter:A1', 'structural-adapter:B1', 'remembering', 'remember', 'ad-intent', 'easy',
    'What is the intent of the Adapter pattern?',
    ['Convert a class’s interface into another interface clients expect, so incompatible types can work together', 'Add new behaviour to an object by wrapping it', 'Control or restrict access to an object', 'Ensure a class has only one instance'], 0,
    'Adapter converts one existing interface into the interface a client expects. The distractors describe Decorator, Proxy, and Singleton.', [GOF, MODULE]),
  mk('structural-adapter:A2', 'structural-adapter:B2', 'understanding', 'understand', 'ad-mechanism', 'moderate',
    'How does an Adapter make two incompatible interfaces work together?',
    ['It edits the adaptee’s source code to match the client', 'It implements the target interface and translates each call to the adaptee’s existing methods', 'It duplicates the adaptee for each client', 'It deletes the target interface entirely'], 1,
    'The adapter conforms to the target interface and forwards/translates calls to the unchanged adaptee. It does not modify or copy the adaptee.', [GOF]),
  mk('structural-adapter:A3', 'structural-adapter:B3', 'applying', 'apply', 'ad-select', 'moderate',
    'A new reporting client expects a JSON-exporter interface, but the only library available exposes a completely different XML API you cannot change. Which pattern lets them work together?',
    ['Decorator', 'Proxy', 'Adapter', 'Composite'], 2,
    'Bridging a required interface to an unchangeable, differently-shaped existing one is Adapter. The distractors add behaviour, control access, or build trees.', [GOF]),
  mk('structural-adapter:A4', 'structural-adapter:B4', 'analyzing', 'analyze', 'ad-identify', 'difficult',
    'Examine:  class JsonTarget { public: virtual std::string toJson() = 0; };  class XmlLib { public: std::string toXml(); };  class XmlToJson : public JsonTarget { XmlLib lib; public: std::string toJson() override { return convert(lib.toXml()); } };  Which pattern is this?',
    ['Bridge', 'Facade', 'Proxy', 'Adapter'], 3,
    'A class implementing the expected interface (JsonTarget) by translating to an existing, differently-shaped API (XmlLib) is Adapter.', [GOF]),
  mk('structural-adapter:A5', 'structural-adapter:B5', 'evaluating', 'evaluate', 'ad-evaluate', 'difficult',
    'A class claims to be an Adapter but simply adds caching around the very same interface the client already used, converting nothing. Is this correctly an Adapter?',
    ['No — Adapter must convert one interface into another; adding behaviour over the same interface is Decorator/Proxy, not Adapter', 'Yes — any wrapper is an Adapter', 'Yes — caching counts as adapting', 'No — Adapters cannot use composition'], 0,
    'Adapter’s defining purpose is interface conversion. Adding behaviour while keeping the same interface is Decorator/Proxy intent, not Adapter.', [GOF, MODULE]),
];
const adapterB: Form = [
  mk('structural-adapter:B1', 'structural-adapter:A1', 'remembering', 'remember', 'ad-intent', 'easy',
    'The Adapter pattern is best described as a way to ___.',
    ['guarantee a single shared instance', 'make one existing interface usable through a different, expected interface', 'share intrinsic state across many objects', 'pass a request along a chain of handlers'], 1,
    'Adapter makes an existing interface usable through the interface a client expects. The distractors are Singleton, Flyweight, and Chain of Responsibility.', [GOF, MODULE]),
  mk('structural-adapter:B2', 'structural-adapter:A2', 'understanding', 'understand', 'ad-mechanism', 'moderate',
    'What is the adapter’s role relative to the adaptee and the client?',
    ['It changes the client to match the adaptee', 'It merges client and adaptee into one class', 'It conforms to the interface the client expects and delegates work to the adaptee', 'It hides the adaptee permanently from all code'], 2,
    'The adapter presents the client-expected interface and delegates to the adaptee, leaving both client and adaptee unchanged.', [GOF]),
  mk('structural-adapter:B3', 'structural-adapter:A3', 'applying', 'apply', 'ad-select', 'moderate',
    'An app’s payment module calls charge(Money), but a third-party SDK you cannot modify only offers processPayment(double, std::string). Which pattern bridges them?',
    ['Composite', 'Facade', 'Decorator', 'Adapter'], 3,
    'Reconciling a required call shape with an unchangeable foreign API is Adapter. The distractors build trees, simplify subsystems, or add behaviour.', [GOF]),
  mk('structural-adapter:B4', 'structural-adapter:A4', 'analyzing', 'analyze', 'ad-identify', 'difficult',
    'Examine:  class Logger { public: virtual void log(std::string) = 0; };  class LegacyWriter { public: void writeLine(const char*); };  class LegacyLogAdapter : public Logger { LegacyWriter w; public: void log(std::string s) override { w.writeLine(s.c_str()); } };  Which pattern is this?',
    ['Adapter', 'Decorator', 'Proxy', 'Bridge'], 0,
    'Implementing the expected Logger interface by translating to a legacy method with a different signature is Adapter.', [GOF]),
  mk('structural-adapter:B5', 'structural-adapter:A5', 'evaluating', 'evaluate', 'ad-evaluate', 'difficult',
    'A wrapper implements the expected RateProvider interface and internally calls an unrelated CurrencyApi with a different signature, translating the arguments. Is calling this an Adapter defensible?',
    ['No — Adapters cannot translate arguments', 'Yes — it implements the expected interface and converts calls to a different existing interface, which is exactly Adapter', 'No — this is always Facade', 'Yes — but only if it also adds new features'], 1,
    'Implementing the expected interface while converting calls to a differently-shaped existing API is the definition of Adapter; argument translation is normal.', [GOF]),
];

// ================================ Proxy ================================
const proxyA: Form = [
  mk('structural-proxy:A1', 'structural-proxy:B1', 'remembering', 'remember', 'px-intent', 'easy',
    'What is the intent of the Proxy pattern?',
    ['Convert one interface into another', 'Add behaviour by stacking wrappers', 'Provide a surrogate or placeholder that controls access to another object', 'Compose objects into part-whole trees'], 2,
    'Proxy supplies a stand-in that controls access to a real subject. The distractors are Adapter, Decorator, and Composite.', [GOF, MODULE]),
  mk('structural-proxy:A2', 'structural-proxy:B2', 'understanding', 'understand', 'px-relate', 'moderate',
    'How does a proxy typically relate to its real subject?',
    ['It exposes a different interface than the subject', 'It inherits the subject’s private data directly', 'It replaces the subject permanently', 'It implements the same interface as the subject and forwards requests, optionally adding access control'], 3,
    'A proxy shares the subject’s interface so it is interchangeable, forwarding calls while controlling access (lazy load, protection, remoting).', [GOF]),
  mk('structural-proxy:A3', 'structural-proxy:B3', 'applying', 'apply', 'px-select', 'moderate',
    'A large image is expensive to load; you want a stand-in that exposes the same interface but defers loading until the image is actually drawn. Which pattern?',
    ['Proxy', 'Decorator', 'Adapter', 'Composite'], 0,
    'A same-interface placeholder that delays creation of the real object is a (virtual) Proxy. The distractors add behaviour, convert, or build trees.', [GOF]),
  mk('structural-proxy:A4', 'structural-proxy:B4', 'analyzing', 'analyze', 'px-identify', 'difficult',
    'Examine:  class Image { public: virtual void draw() = 0; };  class ProxyImage : public Image { RealImage* real = nullptr; std::string file; public: void draw() override { if (!real) real = new RealImage(file); real->draw(); } };  Which pattern is this?',
    ['Adapter', 'Proxy', 'Flyweight', 'Bridge'], 1,
    'Same-interface wrapper that lazily creates and delegates to the real subject is a virtual Proxy.', [GOF]),
  mk('structural-proxy:A5', 'structural-proxy:B5', 'evaluating', 'evaluate', 'px-evaluate', 'difficult',
    'A wrapper implements the subject’s interface and adds logging plus access checks before delegating, without changing the operation’s result or adding new client-facing features. Best classified as?',
    ['Decorator — any wrapper that does extra work is a Decorator', 'Adapter — it converts the interface', 'Proxy — it controls access to the same operation without adding new client-facing behaviour', 'Composite — it manages children'], 2,
    'Controlling access (auth/logging) to the same operation, without new client-facing responsibilities, is Proxy intent, distinguishing it from Decorator.', [GOF, MODULE]),
];
const proxyB: Form = [
  mk('structural-proxy:B1', 'structural-proxy:A1', 'remembering', 'remember', 'px-intent', 'easy',
    'Proxy’s core purpose is to ___.',
    ['share state among many fine-grained objects', 'define a family of interchangeable algorithms', 'simplify a subsystem behind one interface', 'act as a controlled stand-in for another object'], 3,
    'Proxy is a controlled surrogate for another object. The distractors are Flyweight, Strategy, and Facade.', [GOF, MODULE]),
  mk('structural-proxy:B2', 'structural-proxy:A2', 'understanding', 'understand', 'px-relate', 'moderate',
    'What distinguishes a Proxy from a Decorator in intent?',
    ['A Proxy controls or manages access to the subject, while a Decorator adds new responsibilities', 'They are identical in intent', 'A Proxy always adds new features', 'A Decorator changes the object’s interface'], 0,
    'Both wrap a same-interface object, but Proxy’s intent is access control/management whereas Decorator’s is adding behaviour.', [GOF]),
  mk('structural-proxy:B3', 'structural-proxy:A3', 'applying', 'apply', 'px-select', 'moderate',
    'Remote service calls are costly; you want a local object with the same interface that transparently handles the connection and caches results. Which pattern?',
    ['Flyweight', 'Proxy', 'Adapter', 'Mediator'], 1,
    'A same-interface local stand-in for a remote object (managing connection/caching) is a remote Proxy.', [GOF]),
  mk('structural-proxy:B4', 'structural-proxy:A4', 'analyzing', 'analyze', 'px-identify', 'difficult',
    'Examine:  class BankAccount { public: virtual void withdraw(int) = 0; };  class ProtectedAccount : public BankAccount { RealAccount acc; User u; public: void withdraw(int n) override { if (u.isOwner()) acc.withdraw(n); } };  Which pattern is this?',
    ['Decorator', 'Bridge', 'Proxy', 'Strategy'], 2,
    'A same-interface wrapper that performs an access check before delegating is a protection Proxy.', [GOF]),
  mk('structural-proxy:B5', 'structural-proxy:A5', 'evaluating', 'evaluate', 'px-evaluate', 'difficult',
    'A class wraps a Database with the same interface and simply forwards every call unchanged — no access control, lazy initialization, or remoting. Is “Proxy” the right label?',
    ['Yes — any same-interface wrapper is a Proxy', 'Yes — forwarding is the whole point', 'No — Proxy requires changing the interface', 'No — with no access control, lazy loading, or remoting it serves no proxy purpose'], 3,
    'A pure pass-through adds none of the access-management responsibilities that define a Proxy, so the label is not meaningful here.', [GOF, MODULE]),
];

// ================================ Decorator ================================
const decoratorA: Form = [
  mk('structural-decorator:A1', 'structural-decorator:B1', 'remembering', 'remember', 'dc-intent', 'easy',
    'What is the intent of the Decorator pattern?',
    ['Attach additional responsibilities to an object dynamically by wrapping it', 'Convert one interface into another', 'Ensure a class has one instance', 'Centralise communication between objects'], 0,
    'Decorator adds responsibilities to an object dynamically through wrapping. The distractors are Adapter, Singleton, and Mediator.', [GOF, MODULE]),
  mk('structural-decorator:A2', 'structural-decorator:B2', 'understanding', 'understand', 'dc-stack', 'moderate',
    'Why can decorators be stacked on top of one another?',
    ['Because they edit the base class', 'Each decorator implements the same component interface and holds a component, so they nest', 'Because they share one global instance', 'Because they convert interfaces between layers'], 1,
    'Each decorator is itself a component and wraps a component, so wrappers nest indefinitely while staying interchangeable with the base.', [GOF]),
  mk('structural-decorator:A3', 'structural-decorator:B3', 'applying', 'apply', 'dc-select', 'moderate',
    'You must add optional, combinable features (encryption, compression, buffering) to a data stream at runtime without a subclass explosion, keeping the same stream interface. Which pattern?',
    ['Proxy', 'Adapter', 'Decorator', 'Facade'], 2,
    'Dynamically composable responsibilities over a shared interface is Decorator. The distractors control access, convert, or simplify subsystems.', [GOF]),
  mk('structural-decorator:A4', 'structural-decorator:B4', 'analyzing', 'analyze', 'dc-identify', 'difficult',
    'Examine:  class Coffee { public: virtual int cost() = 0; };  class Basic : public Coffee { int cost() override { return 2; } };  class MilkDecorator : public Coffee { Coffee* inner; public: MilkDecorator(Coffee* c) : inner(c) {} int cost() override { return inner->cost() + 1; } };  Which pattern is this?',
    ['Composite', 'Adapter', 'Proxy', 'Decorator'], 3,
    'A same-interface wrapper that augments the wrapped object’s result (adds cost) is Decorator.', [GOF]),
  mk('structural-decorator:A5', 'structural-decorator:B5', 'evaluating', 'evaluate', 'dc-evaluate', 'difficult',
    'A wrapper implements the same interface and adds a visible new feature (drawing a border) to what the client sees, delegating the rest. Best classified as?',
    ['Decorator — it adds new client-facing behaviour while preserving the interface', 'Proxy — any wrapper controls access', 'Adapter — it converts the interface', 'Bridge — it splits abstraction from implementation'], 0,
    'Adding new client-facing responsibility while keeping the component interface is Decorator, distinct from Proxy’s access-control intent.', [GOF, MODULE]),
];
const decoratorB: Form = [
  mk('structural-decorator:B1', 'structural-decorator:A1', 'remembering', 'remember', 'dc-intent', 'easy',
    'Decorator lets you ___.',
    ['guarantee a single instance', 'add responsibilities to individual objects dynamically without affecting other objects', 'convert one interface to another', 'share intrinsic state across objects'], 1,
    'Decorator adds responsibilities to individual objects at runtime. The distractors are Singleton, Adapter, and Flyweight.', [GOF, MODULE]),
  mk('structural-decorator:B2', 'structural-decorator:A2', 'understanding', 'understand', 'dc-stack', 'moderate',
    'What must a decorator share with the object it wraps?',
    ['A common base data structure', 'A single global instance', 'The same abstract interface, so it is interchangeable with the wrapped object', 'A different interface to force conversion'], 2,
    'Sharing the component interface is what lets a decorated object be used anywhere the undecorated one is, and lets wrappers nest.', [GOF]),
  mk('structural-decorator:B3', 'structural-decorator:A3', 'applying', 'apply', 'dc-select', 'moderate',
    'A text view should optionally gain scrollbars and borders in any combination chosen at runtime, all usable wherever a plain view is expected. Which pattern?',
    ['Facade', 'Adapter', 'Proxy', 'Decorator'], 3,
    'Runtime-composable, interchangeable add-ons over a shared interface is Decorator.', [GOF]),
  mk('structural-decorator:B4', 'structural-decorator:A4', 'analyzing', 'analyze', 'dc-identify', 'difficult',
    'Examine:  class Notifier { public: virtual void send(std::string) = 0; };  class Email : public Notifier { void send(std::string) override; };  class SmsDecorator : public Notifier { Notifier* wrap; public: SmsDecorator(Notifier* n) : wrap(n) {} void send(std::string m) override { wrap->send(m); sendSms(m); } };  Which pattern is this?',
    ['Decorator', 'Proxy', 'Composite', 'Adapter'], 0,
    'A same-interface wrapper that performs the wrapped action and adds extra behaviour (an SMS) is Decorator.', [GOF]),
  mk('structural-decorator:B5', 'structural-decorator:A5', 'evaluating', 'evaluate', 'dc-evaluate', 'difficult',
    'A wrapper with the same interface only adds lazy initialization and access checks, not new client-facing features. Is “Decorator” the right classification?',
    ['Yes — every wrapper is a Decorator', 'No — controlling access without adding new client-facing behaviour is Proxy, not Decorator', 'Yes — lazy init is a new feature, so Decorator', 'No — it must be an Adapter'], 1,
    'Access control/lazy loading without new responsibilities is Proxy intent; Decorator specifically adds new client-facing behaviour.', [GOF, MODULE]),
];

// ================================ Composite ================================
const compositeA: Form = [
  mk('structural-composite:A1', 'structural-composite:B1', 'remembering', 'remember', 'cp-intent', 'easy',
    'What is the intent of the Composite pattern?',
    ['Convert interfaces between layers', 'Add behaviour by wrapping', 'Compose objects into part-whole trees and let clients treat individual objects and compositions uniformly', 'Provide a surrogate for access control'], 2,
    'Composite builds part-whole trees treated uniformly. The distractors are Adapter, Decorator, and Proxy.', [GOF, MODULE]),
  mk('structural-composite:A2', 'structural-composite:B2', 'understanding', 'understand', 'cp-uniform', 'moderate',
    'What makes uniform treatment of leaves and composites possible?',
    ['Leaves inherit from composites', 'Composites store no children', 'A global registry maps node types', 'Both leaf and composite implement the same component interface'], 3,
    'A shared component interface lets clients call the same operations on a leaf or a whole subtree without distinguishing them.', [GOF]),
  mk('structural-composite:A3', 'structural-composite:B3', 'applying', 'apply', 'cp-select', 'moderate',
    'A drawing app must treat a single shape and a group of shapes identically for operations like move() and draw(), with groups nesting arbitrarily. Which pattern?',
    ['Composite', 'Decorator', 'Proxy', 'Strategy'], 0,
    'Uniform treatment of single items and nested groups is Composite. The distractors add behaviour, control access, or swap algorithms.', [GOF]),
  mk('structural-composite:A4', 'structural-composite:B4', 'analyzing', 'analyze', 'cp-identify', 'difficult',
    'Examine:  class Node { public: virtual int size() = 0; };  class File : public Node { int size() override { return sz; } };  class Folder : public Node { std::vector<Node*> kids; public: int size() override { int t = 0; for (auto* k : kids) t += k->size(); return t; } };  Which pattern is this?',
    ['Visitor', 'Composite', 'Decorator', 'Iterator'], 1,
    'A shared Node interface where a container recursively aggregates its children is the Composite part-whole structure.', [GOF]),
  mk('structural-composite:A5', 'structural-composite:B5', 'evaluating', 'evaluate', 'cp-evaluate', 'difficult',
    'A “Composite” design gives Folder a children list but File a totally different interface with unrelated method names, forcing clients to type-check each node. Does this satisfy Composite’s intent?',
    ['Yes — as long as there is a tree', 'Yes — composites and leaves should differ', 'No — Composite requires a shared component interface so leaf and composite are treated uniformly; type-checking defeats it', 'No — Composite forbids storing children'], 2,
    'Uniform treatment via a common interface is the whole point; if clients must branch on node type, the pattern’s benefit is lost.', [GOF, MODULE]),
];
const compositeB: Form = [
  mk('structural-composite:B1', 'structural-composite:A1', 'remembering', 'remember', 'cp-intent', 'easy',
    'Composite is used to ___.',
    ['share intrinsic state', 'encapsulate a request as an object', 'define interchangeable algorithms', 'represent part-whole hierarchies so individual and grouped objects are used the same way'], 3,
    'Composite represents part-whole hierarchies treated uniformly. The distractors are Flyweight, Command, and Strategy.', [GOF, MODULE]),
  mk('structural-composite:B2', 'structural-composite:A2', 'understanding', 'understand', 'cp-uniform', 'moderate',
    'In Composite, how does a composite node usually implement an operation?',
    ['It delegates the operation to each of its children and combines the results', 'It ignores its children', 'It converts the child interface', 'It keeps only one child'], 0,
    'A composite forwards the operation to each child and aggregates, which is what makes recursive trees work uniformly.', [GOF]),
  mk('structural-composite:B3', 'structural-composite:A3', 'applying', 'apply', 'cp-select', 'moderate',
    'An org chart must compute total headcount whether you ask an individual employee or a whole department containing sub-departments, using one call. Which pattern?',
    ['Mediator', 'Composite', 'Adapter', 'Facade'], 1,
    'Recursive part-whole aggregation through a common interface is Composite.', [GOF]),
  mk('structural-composite:B4', 'structural-composite:A4', 'analyzing', 'analyze', 'cp-identify', 'difficult',
    'Examine:  class Component { public: virtual double price() = 0; };  class Item : public Component { double price() override { return p; } };  class Box : public Component { std::vector<Component*> items; public: double price() override { double t = 0; for (auto* i : items) t += i->price(); return t; } };  Which pattern is this?',
    ['Decorator', 'Bridge', 'Composite', 'Proxy'], 2,
    'A shared Component interface with a container recursively summing children’s prices is Composite.', [GOF]),
  mk('structural-composite:B5', 'structural-composite:A5', 'evaluating', 'evaluate', 'cp-evaluate', 'difficult',
    'A team models a menu tree where both MenuItem and Menu implement the same MenuComponent interface, and Menu forwards print() to its children. Is this a sound Composite?',
    ['No — Composite needs different interfaces per node', 'No — menus cannot nest', 'Yes — but only with exactly two levels', 'Yes — a shared interface plus recursive delegation to children is exactly Composite’s part-whole structure'], 3,
    'A common interface across leaf and container plus recursive delegation is the correct Composite structure.', [GOF, MODULE]),
];

// ================================ Repository (non-GoF) ================================
const repositoryA: Form = [
  mk('structural-repository:A1', 'structural-repository:B1', 'remembering', 'remember', 'rp-intent', 'easy',
    'What is the purpose of the Repository pattern?',
    ['Provide a collection-like interface that mediates between domain logic and data storage, hiding persistence details', 'Convert one interface into another', 'Ensure a class has one instance', 'Share intrinsic state across objects'], 0,
    'Repository gives domain code a clean, collection-like interface over storage, hiding persistence. The distractors are Adapter, Singleton, and Flyweight.', REPO),
  mk('structural-repository:A2', 'structural-repository:B2', 'understanding', 'understand', 'rp-classify', 'moderate',
    'Repository is NOT one of the original Gang-of-Four design patterns. How is it best classified?',
    ['As a GoF structural pattern like Adapter', 'As an enterprise/application architecture pattern for data access, outside the GoF catalog', 'As a GoF creational pattern', 'As a C++ language idiom'], 1,
    'Repository comes from enterprise/application architecture (Fowler, Evans), not the GoF catalog; it is an application-architecture data-access pattern.', REPO),
  mk('structural-repository:A3', 'structural-repository:B3', 'applying', 'apply', 'rp-select', 'moderate',
    'Domain code should query and save User objects without knowing whether they live in SQL, a file, or an in-memory store, so the data source can be swapped freely. Which approach fits?',
    ['Singleton access', 'Decorator wrapping', 'A Repository abstraction over storage', 'A Facade over the UI'], 2,
    'A domain-facing interface that hides and allows swapping the storage mechanism is the Repository approach.', REPO),
  mk('structural-repository:A4', 'structural-repository:B4', 'analyzing', 'analyze', 'rp-identify', 'difficult',
    'Examine:  class UserRepository { public: virtual User find(int id) = 0; virtual void save(User) = 0; };  class SqlUserRepository : public UserRepository { /* SQL */ };  class InMemoryUserRepository : public UserRepository { /* map */ };  What does this structure most directly demonstrate?',
    ['Interface conversion (Adapter)', 'Access control (Proxy)', 'Algorithm selection (Strategy)', 'A Repository abstracting persistence behind a domain-facing interface'], 3,
    'A domain-facing data-access interface with interchangeable backing stores is the Repository pattern (not GoF).', REPO),
  mk('structural-repository:A5', 'structural-repository:B5', 'evaluating', 'evaluate', 'rp-evaluate', 'difficult',
    'A class named UserRepository builds SQL strings throughout the domain layer and exposes raw connection objects to callers. Does this honor the Repository idea?',
    ['No — Repository’s purpose is to hide persistence behind a clean interface; leaking SQL and connections to the domain defeats the separation', 'Yes — the name makes it a Repository', 'Yes — exposing connections is required', 'No — Repository must be a GoF pattern'], 0,
    'Repository exists to separate domain logic from data-access concerns; leaking SQL/connections breaks that separation regardless of the class name.', REPO),
];
const repositoryB: Form = [
  mk('structural-repository:B1', 'structural-repository:A1', 'remembering', 'remember', 'rp-intent', 'easy',
    'A Repository primarily ___.',
    ['guarantees one instance of the database', 'separates domain logic from data-access concerns behind a collection-like interface', 'adds behaviour to objects by wrapping', 'traverses a collection without exposing it'], 1,
    'Repository separates domain logic from persistence behind a collection-like interface. The distractors are Singleton, Decorator, and Iterator.', REPO),
  mk('structural-repository:B2', 'structural-repository:A2', 'understanding', 'understand', 'rp-classify', 'moderate',
    'Which statement about Repository’s classification is accurate?',
    ['It is the GoF Bridge pattern under another name', 'It is a GoF structural pattern with automated detector support', 'It is a supplemental enterprise architecture pattern, not part of the GoF set, and has no automated GoF-style detector here', 'It is identical to the Proxy pattern'], 2,
    'Repository is an enterprise architecture pattern (not GoF), and CodiNeo provides no automated GoF-style detector for it — conceptual assessment only.', REPO),
  mk('structural-repository:B3', 'structural-repository:A3', 'applying', 'apply', 'rp-select', 'moderate',
    'Tests must run against an in-memory data store while production uses a real database, with identical domain code. Which design enables this substitution cleanly?',
    ['A Singleton database', 'A Decorator chain', 'A Facade', 'A Repository interface with interchangeable implementations'], 3,
    'A Repository interface lets domain code stay the same while the backing store is swapped (in-memory for tests, DB for production).', REPO),
  mk('structural-repository:B4', 'structural-repository:A4', 'analyzing', 'analyze', 'rp-identify', 'difficult',
    'Examine:  class OrderRepository { public: virtual std::optional<Order> byId(int) = 0; virtual std::vector<Order> all() = 0; virtual void add(Order) = 0; };  This domain-facing interface, with swappable backing stores, is an example of ___.',
    ['a Repository abstracting data access', 'an Adapter converting interfaces', 'a Composite tree', 'a Flyweight pool'], 0,
    'A collection-like, domain-facing interface decoupled from a concrete store is the Repository pattern.', REPO),
  mk('structural-repository:B5', 'structural-repository:A5', 'evaluating', 'evaluate', 'rp-evaluate', 'difficult',
    'A team adds a Repository interface, but every method returns raw database rows and requires callers to manage transactions and SQL. Is this a faithful Repository?',
    ['Yes — Repositories should expose SQL', 'No — leaking raw rows, transactions, and SQL to callers breaks the abstraction the pattern exists to provide', 'Yes — as long as it is named Repository', 'No — Repositories may not return data at all'], 1,
    'Repository must insulate the domain from data-access mechanics; exposing rows/transactions/SQL defeats its purpose.', REPO),
];

// ================================ Bridge ================================
const bridgeA: Form = [
  mk('structural-bridge:A1', 'structural-bridge:B1', 'remembering', 'remember', 'br-intent', 'easy',
    'What is the intent of the Bridge pattern?',
    ['Add behaviour by wrapping', 'Provide a surrogate for access', 'Decouple an abstraction from its implementation so the two can vary independently', 'Compose objects into trees'], 2,
    'Bridge separates an abstraction from its implementation so both vary independently. The distractors are Decorator, Proxy, and Composite.', [GOF, MODULE]),
  mk('structural-bridge:A2', 'structural-bridge:B2', 'understanding', 'understand', 'br-vs-inherit', 'moderate',
    'How does Bridge differ from plain inheritance for handling two dimensions of variation?',
    ['It uses one giant subclass per combination', 'It forbids polymorphism', 'It merges both dimensions into one class', 'It composes the abstraction with a separate implementor hierarchy, avoiding a combinatorial explosion of subclasses'], 3,
    'Bridge composes (not inherits) abstraction with an independent implementor hierarchy, avoiding the N×M subclass explosion.', [GOF]),
  mk('structural-bridge:A3', 'structural-bridge:B3', 'applying', 'apply', 'br-select', 'moderate',
    'Shapes (circle, square) must render via multiple drawing backends (OpenGL, SVG), and both sets will keep growing independently. Which pattern avoids one subclass per shape×backend pair?',
    ['Bridge', 'Decorator', 'Adapter', 'Composite'], 0,
    'Two independently-varying dimensions handled by composition is Bridge. The distractors add behaviour, convert, or build trees.', [GOF]),
  mk('structural-bridge:A4', 'structural-bridge:B4', 'analyzing', 'analyze', 'br-identify', 'difficult',
    'Examine:  class Renderer { public: virtual void drawCircle(float) = 0; };  class Shape { protected: Renderer* r; public: Shape(Renderer* rr) : r(rr) {} virtual void draw() = 0; };  class Circle : public Shape { float rad; public: void draw() override { r->drawCircle(rad); } };  Which pattern is this?',
    ['Strategy', 'Bridge', 'Proxy', 'Template Method'], 1,
    'An abstraction hierarchy (Shape) holding and delegating to a separate implementor hierarchy (Renderer) that both vary independently is Bridge.', [GOF]),
  mk('structural-bridge:A5', 'structural-bridge:B5', 'evaluating', 'evaluate', 'br-evaluate', 'difficult',
    'A developer claims a class uses Bridge because it holds a pointer to an implementation object. But there is only one implementation, used solely to hide private members from the header. Is this Bridge?',
    ['Yes — any pointer-to-impl is Bridge', 'Yes — Bridge and PIMPL are the same', 'No — that is the PIMPL idiom; Bridge requires independently varying abstraction and implementation hierarchies', 'No — Bridge cannot use pointers'], 2,
    'A single fixed implementation pointer used only to hide members is the PIMPL idiom; Bridge needs genuine, independently varying abstraction and implementor hierarchies.', [GOF, MODULE]),
];
const bridgeB: Form = [
  mk('structural-bridge:B1', 'structural-bridge:A1', 'remembering', 'remember', 'br-intent', 'easy',
    'Bridge’s intent is to ___.',
    ['share intrinsic state across objects', 'encapsulate requests as objects', 'traverse a collection', 'separate an abstraction from its implementation so each varies independently'], 3,
    'Bridge separates abstraction from implementation so each side changes independently. The distractors are Flyweight, Command, and Iterator.', [GOF, MODULE]),
  mk('structural-bridge:B2', 'structural-bridge:A2', 'understanding', 'understand', 'br-vs-inherit', 'moderate',
    'What distinguishes Bridge from the PIMPL idiom?',
    ['Bridge supports multiple interchangeable implementations of an abstraction, while PIMPL hides one implementation to stabilise a header', 'They are identical', 'PIMPL allows many implementations; Bridge allows only one', 'Bridge is a C++-only idiom'], 0,
    'Bridge’s implementor hierarchy genuinely varies; PIMPL has a single hidden implementation whose purpose is header insulation.', [GOF, MODULE]),
  mk('structural-bridge:B3', 'structural-bridge:A3', 'applying', 'apply', 'br-select', 'moderate',
    'A messaging abstraction (alert, reminder) must work over several delivery channels (email, SMS, push), where both the message types and the channels expand over time, independently. Which pattern?',
    ['Adapter', 'Bridge', 'Facade', 'Memento'], 1,
    'Two independently-growing dimensions (message kinds × channels) handled by composition is Bridge.', [GOF]),
  mk('structural-bridge:B4', 'structural-bridge:A4', 'analyzing', 'analyze', 'br-identify', 'difficult',
    'Examine:  class Device { public: virtual void on() = 0; };  class Remote { protected: Device* dev; public: Remote(Device* d) : dev(d) {} virtual void power() { dev->on(); } };  class TV : public Device { void on() override; };  Which pattern is this?',
    ['Decorator', 'Proxy', 'Bridge', 'Composite'], 2,
    'An abstraction (Remote) composed with an independent implementor hierarchy (Device) that both vary is Bridge.', [GOF]),
  mk('structural-bridge:B5', 'structural-bridge:A5', 'evaluating', 'evaluate', 'br-evaluate', 'difficult',
    'A design has an abstract Shape hierarchy, each holding a pointer to an abstract Renderer hierarchy, and new shapes and new renderers are added without touching each other. Is Bridge a correct description?',
    ['No — this is Strategy only', 'No — Bridge needs a single implementation', 'Yes — but only if there is one renderer', 'Yes — an abstraction hierarchy composed with an independently varying implementor hierarchy is exactly Bridge'], 3,
    'Two varying hierarchies joined by composition, each extensible without the other, is the defining Bridge structure.', [GOF, MODULE]),
];

// ================================ Facade ================================
const facadeA: Form = [
  mk('structural-facade:A1', 'structural-facade:B1', 'remembering', 'remember', 'fc-intent', 'easy',
    'What is the intent of the Facade pattern?',
    ['Provide a single, simplified interface to a larger body of code or a complex subsystem', 'Convert one interface to another', 'Ensure a single instance', 'Add responsibilities by wrapping'], 0,
    'Facade offers a simplified entry point to a subsystem. The distractors are Adapter, Singleton, and Decorator.', [GOF, MODULE]),
  mk('structural-facade:A2', 'structural-facade:B2', 'understanding', 'understand', 'fc-access', 'moderate',
    'Does a Facade hide or remove the subsystem’s classes?',
    ['It permanently removes the subsystem classes', 'It provides a simpler entry point while the subsystem classes remain accessible for advanced use', 'It converts each subsystem class’s interface', 'It guarantees only one subsystem object exists'], 1,
    'A Facade adds a convenient interface but does not forbid direct use of the subsystem when needed.', [GOF]),
  mk('structural-facade:A3', 'structural-facade:B3', 'applying', 'apply', 'fc-select', 'moderate',
    'Clients must start a video conversion with one easy call, while behind it many components (codec, audio mixer, muxer) cooperate. Which pattern offers the simple front door?',
    ['Adapter', 'Proxy', 'Facade', 'Bridge'], 2,
    'A single simplified entry point over a multi-part subsystem is Facade. The distractors convert, control access, or split abstraction.', [GOF]),
  mk('structural-facade:A4', 'structural-facade:B4', 'analyzing', 'analyze', 'fc-identify', 'difficult',
    'Examine:  class ComputerStarter { CPU cpu; Memory mem; Disk disk; public: void boot() { cpu.freeze(); mem.load(0, disk.read(0)); cpu.jump(0); cpu.execute(); } };  A single class offering one method that orchestrates several subsystem objects illustrates which pattern?',
    ['Mediator', 'Composite', 'Adapter', 'Facade'], 3,
    'One class wrapping a multi-step subsystem interaction behind a simple method (boot) is a Facade.', [GOF]),
  mk('structural-facade:A5', 'structural-facade:B5', 'evaluating', 'evaluate', 'fc-evaluate', 'difficult',
    'A class exposes one boot() method that calls several subsystem objects in sequence so callers don’t deal with them directly, but the subsystems don’t communicate back through it. Best classified as?',
    ['Facade — it simplifies one-way access to a subsystem without mediating peer-to-peer communication', 'Mediator — any coordinator is a Mediator', 'Adapter — it converts interfaces', 'Proxy — it controls access'], 0,
    'One-way simplification of subsystem access is Facade; Mediator would coordinate two-way communication among peers.', [GOF, MODULE]),
];
const facadeB: Form = [
  mk('structural-facade:B1', 'structural-facade:A1', 'remembering', 'remember', 'fc-intent', 'easy',
    'A Facade primarily ___.',
    ['shares intrinsic state across objects', 'offers a simple, unified interface over a complex subsystem', 'encapsulates a request as an object', 'captures and restores object state'], 1,
    'Facade offers a unified, simple interface over a subsystem. The distractors are Flyweight, Command, and Memento.', [GOF, MODULE]),
  mk('structural-facade:B2', 'structural-facade:A2', 'understanding', 'understand', 'fc-access', 'moderate',
    'Using a Facade, can clients still bypass it to use subsystem classes directly?',
    ['No — the subsystem becomes private forever', 'No — the Facade deletes the subsystem', 'Yes — a Facade adds a convenient interface without forbidding direct subsystem access', 'Only if the Facade is a Singleton'], 2,
    'A Facade is additive: it simplifies common use but leaves the subsystem reachable for advanced needs.', [GOF]),
  mk('structural-facade:B3', 'structural-facade:A3', 'applying', 'apply', 'fc-select', 'moderate',
    'A home-theatre app should let users press one “watch movie” button that configures amplifier, lights, projector, and player, hiding the steps. Which pattern?',
    ['Decorator', 'Bridge', 'Proxy', 'Facade'], 3,
    'A one-call simplified front over many subsystem steps is Facade.', [GOF]),
  mk('structural-facade:B4', 'structural-facade:A4', 'analyzing', 'analyze', 'fc-identify', 'difficult',
    'Examine:  class OrderProcessor { Inventory inv; Payment pay; Shipping ship; public: void place(Order o) { inv.reserve(o); pay.charge(o); ship.dispatch(o); } };  A single class offering one method that coordinates several subsystems for the client is an example of ___.',
    ['Facade', 'Strategy', 'Observer', 'Flyweight'], 0,
    'Wrapping a multi-subsystem workflow behind one method (place) is a Facade.', [GOF]),
  mk('structural-facade:B5', 'structural-facade:A5', 'evaluating', 'evaluate', 'fc-evaluate', 'difficult',
    'A so-called Facade not only simplifies calls but also forces all subsystem objects to communicate exclusively through it, coordinating their mutual interactions. Is “Facade” still the most precise label?',
    ['Yes — Facade always coordinates communication', 'No — centralising mutual communication between objects is Mediator’s intent, not Facade’s', 'Yes — Facade and Mediator are identical', 'No — this is actually Adapter'], 1,
    'Once it governs two-way communication among objects, the intent matches Mediator; Facade is strictly one-way simplification.', [GOF, MODULE]),
];

// ================================ Flyweight ================================
const flyweightA: Form = [
  mk('structural-flyweight:A1', 'structural-flyweight:B1', 'remembering', 'remember', 'fw-intent', 'easy',
    'What is the intent of the Flyweight pattern?',
    ['Convert interfaces', 'Add behaviour by wrapping', 'Share common state to support large numbers of fine-grained objects efficiently', 'Provide a surrogate for access'], 2,
    'Flyweight minimises memory by sharing state across many similar objects. The distractors are Adapter, Decorator, and Proxy.', [GOF, MODULE]),
  mk('structural-flyweight:A2', 'structural-flyweight:B2', 'understanding', 'understand', 'fw-state', 'moderate',
    'What is the difference between intrinsic and extrinsic state in Flyweight?',
    ['Intrinsic is per-client; extrinsic is shared', 'Both are stored in the flyweight', 'Both are supplied by the client each call', 'Intrinsic state is shared and stored in the flyweight; extrinsic state is context-dependent and supplied by the client'], 3,
    'Intrinsic (shared) state lives in the flyweight; extrinsic (context) state is passed in by the client per operation.', [GOF]),
  mk('structural-flyweight:A3', 'structural-flyweight:B3', 'applying', 'apply', 'fw-select', 'moderate',
    'A text editor renders millions of character glyphs; each glyph’s font and shape repeat, but its position differs. How do you cut memory by sharing the repeating data?',
    ['Flyweight', 'Composite', 'Proxy', 'Decorator'], 0,
    'Sharing repeated intrinsic data while supplying per-use position is Flyweight. The distractors build trees, control access, or add behaviour.', [GOF]),
  mk('structural-flyweight:A4', 'structural-flyweight:B4', 'analyzing', 'analyze', 'fw-identify', 'difficult',
    'Examine:  class Glyph { char c; Font* font; public: void draw(int x, int y); };  Glyph* GlyphFactory::get(char c) { if (!pool.count(c)) pool[c] = new Glyph(c); return pool[c]; }  Position (x, y) is passed to draw(). Which pattern is this?',
    ['Prototype', 'Flyweight', 'Singleton', 'Adapter'], 1,
    'A factory pooling shared instances by intrinsic key (char/font) while position is passed in (extrinsic) is Flyweight.', [GOF]),
  mk('structural-flyweight:A5', 'structural-flyweight:B5', 'evaluating', 'evaluate', 'fw-evaluate', 'difficult',
    'A “Flyweight” stores each object’s unique screen position as a member and never shares instances, creating one object per character. Does this achieve Flyweight’s goal?',
    ['Yes — Flyweight stores all state internally', 'Yes — one object per character is the goal', 'No — keeping per-instance extrinsic state and not sharing defeats Flyweight; position should be extrinsic and instances shared', 'No — Flyweight forbids a factory'], 2,
    'Flyweight’s memory savings come from sharing instances and externalising context state; storing position per instance and not sharing negates the benefit.', [GOF, MODULE]),
];
const flyweightB: Form = [
  mk('structural-flyweight:B1', 'structural-flyweight:A1', 'remembering', 'remember', 'fw-intent', 'easy',
    'Flyweight is used to ___.',
    ['encapsulate a request as an object', 'define interchangeable algorithms', 'traverse a collection', 'support large numbers of similar objects efficiently by sharing their common state'], 3,
    'Flyweight supports many similar objects by sharing common state. The distractors are Command, Strategy, and Iterator.', [GOF, MODULE]),
  mk('structural-flyweight:B2', 'structural-flyweight:A2', 'understanding', 'understand', 'fw-state', 'moderate',
    'Where is extrinsic state kept in a Flyweight design?',
    ['Outside the flyweight — supplied by the client or context when an operation runs', 'Permanently inside each shared flyweight', 'In a single global Singleton only', 'Nowhere — extrinsic state is discarded'], 0,
    'Extrinsic state is context-dependent and provided by the client at call time, so it must not be stored in the shared flyweight.', [GOF]),
  mk('structural-flyweight:B3', 'structural-flyweight:A3', 'applying', 'apply', 'fw-select', 'moderate',
    'A forest scene draws 1,000,000 trees of a few species; mesh and texture repeat per species while location varies per tree. How do you avoid storing the mesh a million times?',
    ['Composite', 'Flyweight', 'Bridge', 'Memento'], 1,
    'Sharing per-species intrinsic data while passing per-tree position is Flyweight.', [GOF]),
  mk('structural-flyweight:B4', 'structural-flyweight:A4', 'analyzing', 'analyze', 'fw-identify', 'difficult',
    'Examine:  class TreeType { std::string name; Texture tex; public: void render(int x, int y); };  TreeType* TreeFactory::of(std::string n) { auto it = pool.find(n); if (it != pool.end()) return it->second; return pool[n] = new TreeType(n); }  Which pattern is this?',
    ['Prototype', 'Builder', 'Flyweight', 'Proxy'], 2,
    'A factory returning shared instances keyed by intrinsic data, with position passed to render(), is Flyweight.', [GOF]),
  mk('structural-flyweight:B5', 'structural-flyweight:A5', 'evaluating', 'evaluate', 'fw-evaluate', 'difficult',
    'A renderer keeps one shared ParticleType (color, sprite) per kind and passes each particle’s x/y/velocity at draw time. Is this a correct Flyweight?',
    ['No — Flyweight must store position internally', 'No — Flyweight cannot use a factory', 'Yes — but only for fewer than 100 objects', 'Yes — sharing intrinsic data while passing extrinsic state at call time is exactly Flyweight'], 3,
    'Shared intrinsic data plus client-supplied extrinsic state at call time is the correct Flyweight design.', [GOF, MODULE]),
];

export const STRUCTURAL_ASSESSMENT_FORMS: Record<string, { A: Form; B: Form }> = {
  'structural-adapter': { A: adapterA, B: adapterB },
  'structural-proxy': { A: proxyA, B: proxyB },
  'structural-decorator': { A: decoratorA, B: decoratorB },
  'structural-composite': { A: compositeA, B: compositeB },
  'structural-repository': { A: repositoryA, B: repositoryB },
  'structural-bridge': { A: bridgeA, B: bridgeB },
  'structural-facade': { A: facadeA, B: facadeB },
  'structural-flyweight': { A: flyweightA, B: flyweightB },
};
