// Authored Behavioural formal Form A/B objective items (Remember→Evaluate).
// Create stays in each module's practical C++ task — never an MCQ. Items focus
// on intent, responsibilities, interaction, communication, state transitions,
// request handling, traversal, and collaboration (not class naming). All items
// validationStatus 'draft'. Correct-option positions spread across A/B/C/D.

import type { ObjectiveAssessmentQuestion } from '../learningModules';

type Form = ReadonlyArray<ObjectiveAssessmentQuestion>;

const GOF = 'Gamma, Helm, Johnson, Vlissides (1994), Design Patterns';
const MODULE = 'CodiNeo Behavioural module content';

const mk = (
  id: string, paired: string,
  taxonomy: 'remembering' | 'understanding' | 'applying' | 'analyzing' | 'evaluating',
  bloomLevel: 'remember' | 'understand' | 'apply' | 'analyze' | 'evaluate',
  competencyId: string, difficulty: 'easy' | 'moderate' | 'difficult',
  question: string, options: [string, string, string, string], correctIndex: 0 | 1 | 2 | 3,
  rationale: string, sources: ReadonlyArray<string>,
): ObjectiveAssessmentQuestion => ({
  id, form: id.includes(':A') ? 'A' : 'B', pairedQuestionId: paired, type: 'mcq', taxonomy, bloomLevel, competencyId, difficulty,
  question, options, correctIndex, rationale, sourceReferences: sources, validationStatus: 'draft',
});

// ================================ Strategy ================================
const strategyA: Form = [
  mk('behavioural-strategy:A1', 'behavioural-strategy:B1', 'remembering', 'remember', 'st-intent', 'easy',
    'What is the intent of the Strategy pattern?',
    ['Define a family of interchangeable algorithms and make them swappable at runtime', 'Convert one interface into another', 'Ensure a single instance', 'Capture and restore object state'], 0,
    'Strategy encapsulates interchangeable algorithms behind a common interface. The distractors are Adapter, Singleton, and Memento.', [GOF, MODULE]),
  mk('behavioural-strategy:A2', 'behavioural-strategy:B2', 'understanding', 'understand', 'st-mechanism', 'moderate',
    'How does Strategy let the algorithm change without changing the client?',
    ['The client edits the algorithm’s code each time', 'The client depends on a strategy interface and delegates, so concrete algorithms swap freely', 'The client inherits each algorithm', 'The algorithms share one global instance'], 1,
    'The client holds a strategy interface and delegates; concrete strategies are interchangeable behind it.', [GOF]),
  mk('behavioural-strategy:A3', 'behavioural-strategy:B3', 'applying', 'apply', 'st-select', 'moderate',
    'A checkout must compute shipping cost by several methods (flat, weight-based, distance-based), chosen per order at runtime, without if/else sprawl. Which pattern?',
    ['Observer', 'Template Method', 'Strategy', 'State'], 2,
    'Selecting among interchangeable algorithms at runtime is Strategy. Template Method fixes a skeleton; State is driven by internal state; Observer notifies.', [GOF]),
  mk('behavioural-strategy:A4', 'behavioural-strategy:B4', 'analyzing', 'analyze', 'st-identify', 'difficult',
    'Examine:  class SortStrategy { public: virtual void sort(std::vector<int>&) = 0; };  class Quick : public SortStrategy { void sort(std::vector<int>&) override; };  class Sorter { SortStrategy* s; public: Sorter(SortStrategy* x) : s(x) {} void run(std::vector<int>& v) { s->sort(v); } };  Which pattern is this?',
    ['State', 'Command', 'Template Method', 'Strategy'], 3,
    'A context delegating to an injected, interchangeable algorithm object is Strategy.', [GOF]),
  mk('behavioural-strategy:A5', 'behavioural-strategy:B5', 'evaluating', 'evaluate', 'st-evaluate', 'difficult',
    'A design swaps an object’s algorithm via an interface, but the object never changes which one it uses based on its own internal state — the client picks it. Is Strategy or State the better fit?',
    ['Strategy — it is a client-selected interchangeable algorithm, not state-driven behaviour', 'State — any swappable behaviour is State', 'Observer — it notifies dependents', 'Command — it encapsulates a request'], 0,
    'Strategy is a client-chosen algorithm; State changes behaviour as the object’s own state transitions. Here the client selects it, so Strategy fits.', [GOF, MODULE]),
];
const strategyB: Form = [
  mk('behavioural-strategy:B1', 'behavioural-strategy:A1', 'remembering', 'remember', 'st-intent', 'easy',
    'Strategy’s purpose is to ___.',
    ['broadcast changes to many observers', 'make a set of algorithms interchangeable behind a common interface', 'build an object step by step', 'share intrinsic state'], 1,
    'Strategy makes algorithms interchangeable behind one interface. The distractors are Observer, Builder, and Flyweight.', [GOF, MODULE]),
  mk('behavioural-strategy:B2', 'behavioural-strategy:A2', 'understanding', 'understand', 'st-mechanism', 'moderate',
    'What is the relationship between context and strategy?',
    ['The strategy owns and controls the context', 'Context and strategy are the same object', 'The context delegates the varying behaviour to a strategy object it holds', 'The strategy must be a Singleton'], 2,
    'The context delegates the variable behaviour to a held strategy, so swapping strategies changes behaviour without touching the context.', [GOF]),
  mk('behavioural-strategy:B3', 'behavioural-strategy:A3', 'applying', 'apply', 'st-select', 'moderate',
    'A game character’s pathfinding should switch among A*, Dijkstra, and greedy at runtime depending on settings, with the rest of the code unchanged. Which pattern?',
    ['Memento', 'Mediator', 'Visitor', 'Strategy'], 3,
    'Interchangeable algorithms selected at runtime behind one interface is Strategy.', [GOF]),
  mk('behavioural-strategy:B4', 'behavioural-strategy:A4', 'analyzing', 'analyze', 'st-identify', 'difficult',
    'Examine:  class Compressor { public: virtual void compress(File&) = 0; };  class Zip : public Compressor { void compress(File&) override; };  class Archiver { Compressor* c; public: Archiver(Compressor* x) : c(x) {} void store(File& f) { c->compress(f); } };  Which pattern is this?',
    ['Strategy', 'Observer', 'State', 'Adapter'], 0,
    'A context (Archiver) delegating to an injected interchangeable algorithm (Compressor) is Strategy.', [GOF]),
  mk('behavioural-strategy:B5', 'behavioural-strategy:A5', 'evaluating', 'evaluate', 'st-evaluate', 'difficult',
    'A class accepts a payment-algorithm object implementing a common interface and delegates the calculation to it, letting callers pass any algorithm. Is “Strategy” a defensible label?',
    ['No — this must be Template Method', 'Yes — delegating interchangeable algorithms through a common interface chosen by the caller is exactly Strategy', 'No — Strategy requires inheriting the context', 'Yes — but only with exactly two algorithms'], 1,
    'Caller-chosen interchangeable algorithms behind a common interface, delegated by the context, is Strategy.', [GOF]),
];

// ================================ Observer ================================
const observerA: Form = [
  mk('behavioural-observer:A1', 'behavioural-observer:B1', 'remembering', 'remember', 'ob-intent', 'easy',
    'What is the intent of the Observer pattern?',
    ['Convert interfaces', 'Share intrinsic state', 'Define a one-to-many dependency so that when one object changes state, its dependents are notified automatically', 'Encapsulate a request as an object'], 2,
    'Observer establishes one-to-many automatic notification. The distractors are Adapter, Flyweight, and Command.', [GOF, MODULE]),
  mk('behavioural-observer:A2', 'behavioural-observer:B2', 'understanding', 'understand', 'ob-mechanism', 'moderate',
    'How do observers learn about changes in the subject?',
    ['Observers poll a global variable', 'Observers inherit the subject’s data', 'Each observer edits the subject directly', 'The subject holds a list of observers and calls their update method when its state changes'], 3,
    'The subject keeps registered observers and notifies them (calls update) on change; observers do not poll or inherit.', [GOF]),
  mk('behavioural-observer:A3', 'behavioural-observer:B3', 'applying', 'apply', 'ob-select', 'moderate',
    'A spreadsheet cell’s value changes and several charts plus a status bar must refresh automatically, without the cell knowing their concrete types. Which pattern?',
    ['Observer', 'Mediator', 'Command', 'Strategy'], 0,
    'Automatic one-to-many notification with loose coupling to dependents is Observer. The distractors coordinate peers, encapsulate requests, or swap algorithms.', [GOF]),
  mk('behavioural-observer:A4', 'behavioural-observer:B4', 'analyzing', 'analyze', 'ob-identify', 'difficult',
    'Examine:  class Observer { public: virtual void update(int) = 0; };  class Subject { std::vector<Observer*> obs; int v; public: void attach(Observer* o) { obs.push_back(o); } void set(int x) { v = x; for (auto* o : obs) o->update(v); } };  Which pattern is this?',
    ['Mediator', 'Observer', 'Visitor', 'Iterator'], 1,
    'A subject maintaining observers and broadcasting state changes to them is the Observer pattern.', [GOF]),
  mk('behavioural-observer:A5', 'behavioural-observer:B5', 'evaluating', 'evaluate', 'ob-evaluate', 'difficult',
    'Each dependent registers with one source and is notified when that source changes; the dependents do not coordinate each other’s mutual interactions. Best classified as?',
    ['Mediator — any notification is Mediator', 'Command — it encapsulates requests', 'Observer — it is one-to-many change notification, not centralised peer coordination', 'State — behaviour changes with state'], 2,
    'One-to-many change notification is Observer; Mediator would centralise two-way coordination among peers.', [GOF, MODULE]),
];
const observerB: Form = [
  mk('behavioural-observer:B1', 'behavioural-observer:A1', 'remembering', 'remember', 'ob-intent', 'easy',
    'Observer establishes ___.',
    ['a part-whole hierarchy', 'an interchangeable-algorithm family', 'a request encapsulated as an object', 'a one-to-many notification relationship between a subject and its dependents'], 3,
    'Observer is one-to-many change notification. The distractors are Composite, Strategy, and Command.', [GOF, MODULE]),
  mk('behavioural-observer:B2', 'behavioural-observer:A2', 'understanding', 'understand', 'ob-mechanism', 'moderate',
    'Why does the subject depend only on an observer interface, not concrete observers?',
    ['So new observer types can subscribe without modifying the subject (loose coupling)', 'So the subject can delete observers’ data', 'Because observers must be Singletons', 'So the subject can convert their interfaces'], 0,
    'Depending on an abstract observer interface lets new observers subscribe without changing the subject — the loose coupling Observer provides.', [GOF]),
  mk('behavioural-observer:B3', 'behavioural-observer:A3', 'applying', 'apply', 'ob-select', 'moderate',
    'When a stock price updates, a dashboard, a logger, and an alert service must all react immediately, and more subscribers may be added later. Which pattern?',
    ['Composite', 'Observer', 'Facade', 'Template Method'], 1,
    'Dynamic, extensible one-to-many notification is Observer.', [GOF]),
  mk('behavioural-observer:B4', 'behavioural-observer:A4', 'analyzing', 'analyze', 'ob-identify', 'difficult',
    'Examine:  class Listener { public: virtual void onEvent(const Event&) = 0; };  class Button { std::vector<Listener*> ls; public: void addListener(Listener* l) { ls.push_back(l); } void click() { Event e; for (auto* l : ls) l->onEvent(e); } };  Which pattern is this?',
    ['Strategy', 'Command', 'Observer', 'Adapter'], 2,
    'A source maintaining listeners and broadcasting an event to all of them is Observer.', [GOF]),
  mk('behavioural-observer:B5', 'behavioural-observer:A5', 'evaluating', 'evaluate', 'ob-evaluate', 'difficult',
    'A publisher keeps a subscriber list and, on each new message, loops calling subscriber.notify(msg); subscribers can join or leave at runtime. Is this a correct Observer?',
    ['No — Observer needs a central hub coordinating peers', 'No — subscribers cannot change at runtime', 'Yes — but only with one subscriber', 'Yes — maintaining subscribers and broadcasting state changes to them dynamically is exactly Observer'], 3,
    'Dynamic subscription plus broadcast notification of changes is the Observer pattern.', [GOF]),
];

// ================================ Iterator ================================
const iteratorA: Form = [
  mk('behavioural-iterator:A1', 'behavioural-iterator:B1', 'remembering', 'remember', 'it-intent', 'easy',
    'What is the intent of the Iterator pattern?',
    ['Provide a way to access elements of a collection sequentially without exposing its underlying representation', 'Convert interfaces', 'Ensure one instance', 'Add behaviour by wrapping'], 0,
    'Iterator gives sequential access while hiding the collection’s internals. The distractors are Adapter, Singleton, and Decorator.', [GOF, MODULE]),
  mk('behavioural-iterator:A2', 'behavioural-iterator:B2', 'understanding', 'understand', 'it-rationale', 'moderate',
    'Why use an Iterator instead of exposing the collection’s internal structure?',
    ['It makes the collection a Singleton', 'It lets clients traverse uniformly while the collection keeps its representation private and can change it', 'It converts the element type', 'It deletes the collection after traversal'], 1,
    'Iterator decouples traversal from representation, so the collection can hide and later change its internals.', [GOF]),
  mk('behavioural-iterator:A3', 'behavioural-iterator:B3', 'applying', 'apply', 'it-select', 'moderate',
    'You want client code to walk over a custom tree and a custom hash set the same way (begin/next/done) without knowing how each stores data. Which pattern?',
    ['Visitor', 'Composite', 'Iterator', 'Observer'], 2,
    'Uniform sequential traversal that hides each collection’s internal storage is Iterator.', [GOF]),
  mk('behavioural-iterator:A4', 'behavioural-iterator:B4', 'analyzing', 'analyze', 'it-identify', 'difficult',
    'Examine:  class Iterator { public: virtual bool hasNext() = 0; virtual int next() = 0; };  class ListIterator : public Iterator { std::vector<int>& v; size_t i = 0; public: bool hasNext() override { return i < v.size(); } int next() override { return v[i++]; } };  Which pattern is this?',
    ['Strategy', 'Command', 'State', 'Iterator'], 3,
    'An object exposing hasNext/next while tracking position and hiding the container is the Iterator pattern.', [GOF]),
  mk('behavioural-iterator:A5', 'behavioural-iterator:B5', 'evaluating', 'evaluate', 'it-evaluate', 'difficult',
    'A “traversal” design forces clients to read the collection’s internal array index and bounds directly to loop. Does this satisfy Iterator’s intent?',
    ['No — exposing internal representation to clients defeats Iterator’s purpose', 'Yes — direct index access is the Iterator pattern', 'Yes — as long as there is a loop', 'No — Iterator forbids sequential access'], 0,
    'Iterator exists to hide the representation; making clients use internal indices breaks the encapsulation it provides.', [GOF, MODULE]),
];
const iteratorB: Form = [
  mk('behavioural-iterator:B1', 'behavioural-iterator:A1', 'remembering', 'remember', 'it-intent', 'easy',
    'Iterator provides ___.',
    ['a single global instance', 'sequential access to a collection’s elements without revealing its internal structure', 'a one-to-many notification', 'a simplified subsystem interface'], 1,
    'Iterator gives sequential element access while hiding internals. The distractors are Singleton, Observer, and Facade.', [GOF, MODULE]),
  mk('behavioural-iterator:B2', 'behavioural-iterator:A2', 'understanding', 'understand', 'it-rationale', 'moderate',
    'What responsibility moves into the iterator object?',
    ['Owning all the collection’s data', 'Converting element interfaces', 'Tracking the current traversal position, so multiple independent traversals can coexist', 'Notifying observers of changes'], 2,
    'The iterator holds the traversal cursor, which allows several simultaneous, independent traversals of one collection.', [GOF]),
  mk('behavioural-iterator:B3', 'behavioural-iterator:A3', 'applying', 'apply', 'it-select', 'moderate',
    'A library exposes several container types; users should loop through any of them with the same code, and even run two simultaneous passes over one container. Which pattern?',
    ['Memento', 'Facade', 'Bridge', 'Iterator'], 3,
    'Uniform traversal plus independent concurrent passes (separate cursors) is Iterator.', [GOF]),
  mk('behavioural-iterator:B4', 'behavioural-iterator:A4', 'analyzing', 'analyze', 'it-identify', 'difficult',
    'Examine:  class TreeIterator { std::stack<Node*> st; public: TreeIterator(Node* root) { if (root) st.push(root); } bool done() { return st.empty(); } Node* current() { return st.top(); } void advance() { Node* n = st.top(); st.pop(); if (n->right) st.push(n->right); if (n->left) st.push(n->left); } };  Which pattern is this?',
    ['Iterator', 'Visitor', 'Composite', 'Strategy'], 0,
    'An object that maintains traversal state and exposes done/current/advance over a structure is an Iterator.', [GOF]),
  mk('behavioural-iterator:B5', 'behavioural-iterator:A5', 'evaluating', 'evaluate', 'it-evaluate', 'difficult',
    'A collection returns a cursor object exposing hasNext()/next() and keeps its storage (array vs linked list) private and swappable. Is “Iterator” a defensible label?',
    ['No — this must be Strategy', 'Yes — a cursor giving sequential access while the collection hides its representation is exactly Iterator', 'No — Iterator must expose the storage', 'Yes — but only for arrays'], 1,
    'A cursor giving sequential access while the underlying storage stays private and changeable is the Iterator pattern.', [GOF]),
];

// ================================ Command ================================
const commandA: Form = [
  mk('behavioural-command:A1', 'behavioural-command:B1', 'remembering', 'remember', 'cm-intent', 'easy',
    'What is the intent of the Command pattern?',
    ['Convert interfaces', 'Share intrinsic state', 'Encapsulate a request as an object, so you can parameterise, queue, log, and undo operations', 'Traverse a collection'], 2,
    'Command reifies a request as an object enabling queuing, logging, and undo. The distractors are Adapter, Flyweight, and Iterator.', [GOF, MODULE]),
  mk('behavioural-command:A2', 'behavioural-command:B2', 'understanding', 'understand', 'cm-enable', 'moderate',
    'What does turning a request into a Command object enable?',
    ['Faster compilation', 'Automatic multithreading', 'Removing the receiver', 'Storing, queuing, and undoing operations uniformly, and decoupling the invoker from the receiver'], 3,
    'A reified request can be stored, queued, replayed, and undone, and it decouples the invoker from the concrete receiver.', [GOF]),
  mk('behavioural-command:A3', 'behavioural-command:B3', 'applying', 'apply', 'cm-select', 'moderate',
    'A text editor needs undo/redo and a macro recorder; each user action must be storable, replayable, and reversible. Which pattern?',
    ['Command', 'Observer', 'Strategy', 'Iterator'], 0,
    'Reifying actions so they can be stored, replayed, and undone is Command. The distractors notify, swap algorithms, or traverse.', [GOF]),
  mk('behavioural-command:A4', 'behavioural-command:B4', 'analyzing', 'analyze', 'cm-identify', 'difficult',
    'Examine:  class Command { public: virtual void execute() = 0; virtual void undo() = 0; };  class InsertText : public Command { Doc& d; std::string t; public: void execute() override { d.insert(t); } void undo() override { d.erase(t); } };  class Invoker { std::vector<Command*> history; public: void run(Command* c) { c->execute(); history.push_back(c); } };  Which pattern is this?',
    ['Strategy', 'Command', 'State', 'Memento'], 1,
    'Reified requests with execute/undo, run and recorded by an invoker, is the Command pattern.', [GOF]),
  mk('behavioural-command:A5', 'behavioural-command:B5', 'evaluating', 'evaluate', 'cm-evaluate', 'difficult',
    'A design calls receiver methods directly from the UI handler with no request object, yet claims to support undo by re-reading the UI. Is this the Command pattern?',
    ['Yes — any action with undo is Command', 'Yes — direct calls are Command', 'No — Command requires reifying each request as an object (with execute/undo); calling the receiver directly is not Command', 'No — Command forbids an invoker'], 2,
    'Command’s essence is making each request a first-class object; direct receiver calls without command objects are not Command.', [GOF, MODULE]),
];
const commandB: Form = [
  mk('behavioural-command:B1', 'behavioural-command:A1', 'remembering', 'remember', 'cm-intent', 'easy',
    'The Command pattern ___.',
    ['shares intrinsic state', 'wraps an action and its parameters in an object so it can be executed later, queued, or undone', 'defines a one-to-many dependency', 'selects among interchangeable algorithms'], 1,
    'Command wraps an action as an object for later execution, queuing, or undo. The distractors are Flyweight, Observer, and Strategy.', [GOF, MODULE]),
  mk('behavioural-command:B2', 'behavioural-command:A2', 'understanding', 'understand', 'cm-enable', 'moderate',
    'How does Command decouple the invoker from the receiver?',
    ['The invoker triggers an abstract command and never needs to know which receiver or operation it wraps', 'The invoker inherits from the receiver', 'The receiver calls the invoker directly', 'They must be the same object'], 0,
    'The invoker depends only on the abstract command, so it is independent of the concrete receiver and operation.', [GOF]),
  mk('behavioural-command:B3', 'behavioural-command:A3', 'applying', 'apply', 'cm-select', 'moderate',
    'A home-automation remote must map each button to a configurable action, support queuing scheduled actions, and undo the last one. Which pattern?',
    ['Mediator', 'Command', 'Facade', 'Visitor'], 1,
    'Configurable, queueable, undoable actions as objects is Command.', [GOF]),
  mk('behavioural-command:B4', 'behavioural-command:A4', 'analyzing', 'analyze', 'cm-identify', 'difficult',
    'Examine:  class Operation { public: virtual void execute() = 0; };  class TurnOnLight : public Operation { Light& l; public: TurnOnLight(Light& x) : l(x) {} void execute() override { l.on(); } };  class Button { Operation* op; public: void press() { op->execute(); } };  Which pattern is this?',
    ['Observer', 'Strategy', 'Command', 'State'], 2,
    'An action reified as an object that an invoker (Button) triggers without knowing the receiver is Command.', [GOF]),
  mk('behavioural-command:B5', 'behavioural-command:A5', 'evaluating', 'evaluate', 'cm-evaluate', 'difficult',
    'Each user action is an object with execute() and undo(), stored in a history stack the invoker pops to reverse actions. Is “Command” correct?',
    ['No — this is Memento', 'No — Command cannot support undo', 'Yes — but only without a history', 'Yes — reifying requests as execute/undo objects managed via a history is exactly Command'], 3,
    'Reified requests with execute/undo and a history for reversal is the Command pattern (Memento stores state, not requests).', [GOF]),
];

// ================================ Template Method ================================
const templateA: Form = [
  mk('behavioural-template-method:A1', 'behavioural-template-method:B1', 'remembering', 'remember', 'tm-intent', 'easy',
    'What is the intent of the Template Method pattern?',
    ['Define the skeleton of an algorithm in a base method, deferring some steps to subclasses', 'Convert interfaces', 'Ensure one instance', 'Share intrinsic state'], 0,
    'Template Method fixes an algorithm’s skeleton and lets subclasses supply steps. The distractors are Adapter, Singleton, and Flyweight.', [GOF, MODULE]),
  mk('behavioural-template-method:A2', 'behavioural-template-method:B2', 'understanding', 'understand', 'tm-fixedvary', 'moderate',
    'In Template Method, what stays fixed and what varies?',
    ['Everything is overridden', 'The overall step sequence is fixed in the base class while specific steps are overridden by subclasses', 'Nothing can be overridden', 'The client supplies the whole algorithm at runtime'], 1,
    'The invariant sequence lives in the base method; subclasses override only the designated variable steps.', [GOF]),
  mk('behavioural-template-method:A3', 'behavioural-template-method:B3', 'applying', 'apply', 'tm-select', 'moderate',
    'Several report generators share the same overall flow (open → fetch → format → save) but differ only in how they format. You want the flow defined once. Which pattern?',
    ['Strategy', 'Command', 'Template Method', 'Observer'], 2,
    'A fixed algorithm skeleton with a varying step implemented per subclass is Template Method. Strategy would inject the whole algorithm as an object.', [GOF]),
  mk('behavioural-template-method:A4', 'behavioural-template-method:B4', 'analyzing', 'analyze', 'tm-identify', 'difficult',
    'Examine:  class Game { public: void play() { start(); while (!over()) turn(); end(); } protected: virtual void start() = 0; virtual void turn() = 0; virtual bool over() = 0; virtual void end() = 0; };  class Chess : public Game { /* overrides */ };  Which pattern is this?',
    ['Strategy', 'State', 'Command', 'Template Method'], 3,
    'A non-virtual base method fixing the flow and calling overridable steps is Template Method.', [GOF]),
  mk('behavioural-template-method:A5', 'behavioural-template-method:B5', 'evaluating', 'evaluate', 'tm-evaluate', 'difficult',
    'An algorithm’s invariant steps live in a base method that calls overridable hooks; subclasses customise only those hooks. Is this Template Method or Strategy?',
    ['Template Method — variation happens via subclass overrides of fixed hook steps (inheritance), not a swapped strategy object', 'Strategy — any variation is Strategy', 'Observer — it notifies dependents', 'Composite — it forms a tree'], 0,
    'Template Method varies behaviour through subclass overrides of fixed hooks (inheritance); Strategy varies it by composing a swappable object.', [GOF, MODULE]),
];
const templateB: Form = [
  mk('behavioural-template-method:B1', 'behavioural-template-method:A1', 'remembering', 'remember', 'tm-intent', 'easy',
    'Template Method defines ___.',
    ['a one-to-many dependency', 'an algorithm’s skeleton whose selected steps subclasses fill in without changing its structure', 'a surrogate for access control', 'a part-whole hierarchy'], 1,
    'Template Method defines an algorithm skeleton with subclass-filled steps. The distractors are Observer, Proxy, and Composite.', [GOF, MODULE]),
  mk('behavioural-template-method:B2', 'behavioural-template-method:A2', 'understanding', 'understand', 'tm-fixedvary', 'moderate',
    'Where is the invariant control flow located in Template Method?',
    ['In each subclass separately', 'In a separate strategy object', 'In a base-class method that calls the overridable primitive operations in a fixed order', 'In the client code'], 2,
    'The base class owns the fixed control flow and calls the overridable steps in order.', [GOF]),
  mk('behavioural-template-method:B3', 'behavioural-template-method:A3', 'applying', 'apply', 'tm-select', 'moderate',
    'Data importers for CSV, JSON, and XML all follow validate → parse → transform → persist, differing only in parsing. You want to write the sequence once. Which pattern?',
    ['Visitor', 'Mediator', 'Bridge', 'Template Method'], 3,
    'A shared fixed sequence with one varying step per subclass is Template Method.', [GOF]),
  mk('behavioural-template-method:B4', 'behavioural-template-method:A4', 'analyzing', 'analyze', 'tm-identify', 'difficult',
    'Examine:  class Beverage { public: void make() { boil(); brew(); pour(); addExtras(); } protected: void boil(); void pour(); virtual void brew() = 0; virtual void addExtras() = 0; };  class Tea : public Beverage { /* overrides brew, addExtras */ };  Which pattern is this?',
    ['Template Method', 'Strategy', 'Decorator', 'Command'], 0,
    'A fixed make() sequence calling some concrete and some overridable steps is Template Method.', [GOF]),
  mk('behavioural-template-method:B5', 'behavioural-template-method:A5', 'evaluating', 'evaluate', 'tm-evaluate', 'difficult',
    'A base class fixes the sequence run() { setUp(); execute(); tearDown(); } where execute() is abstract and subclasses implement it. Is “Template Method” defensible?',
    ['No — this is Strategy', 'Yes — a fixed skeleton with subclass-supplied steps via overriding is exactly Template Method', 'No — Template Method forbids abstract steps', 'Yes — but only with one subclass'], 1,
    'A fixed skeleton whose variable steps are supplied by subclass overrides is Template Method.', [GOF]),
];

// ================================ State ================================
const stateA: Form = [
  mk('behavioural-state:A1', 'behavioural-state:B1', 'remembering', 'remember', 'stt-intent', 'easy',
    'What is the intent of the State pattern?',
    ['Convert interfaces', 'Share intrinsic state', 'Allow an object to alter its behaviour when its internal state changes, appearing to change its class', 'Traverse a collection'], 2,
    'State lets an object change behaviour as its internal state changes. The distractors are Adapter, Flyweight, and Iterator.', [GOF, MODULE]),
  mk('behavioural-state:A2', 'behavioural-state:B2', 'understanding', 'understand', 'stt-mechanism', 'moderate',
    'How does the State pattern avoid large conditional blocks on a status field?',
    ['By using one giant switch statement', 'By making the object a Singleton', 'By copying the object per state', 'By representing each state as an object and delegating behaviour to the current state, which can switch to another'], 3,
    'Each state becomes an object encapsulating its behaviour and transitions; the context delegates to the current one.', [GOF]),
  mk('behavioural-state:A3', 'behavioural-state:B3', 'applying', 'apply', 'stt-select', 'moderate',
    'A TCP connection behaves differently when Closed, Listening, or Established, and each event may move it to another mode with its own rules. Which pattern?',
    ['State', 'Strategy', 'Observer', 'Command'], 0,
    'Behaviour and transitions driven by the object’s changing mode is State. Strategy is client-chosen and not transition-driven.', [GOF]),
  mk('behavioural-state:A4', 'behavioural-state:B4', 'analyzing', 'analyze', 'stt-identify', 'difficult',
    'Examine:  class State { public: virtual void insertCoin(Machine&) = 0; };  class NoCoin : public State { void insertCoin(Machine& m) override { m.setState(new HasCoin()); } };  class Machine { State* s; public: void setState(State* x) { s = x; } void insertCoin() { s->insertCoin(*this); } };  Which pattern is this?',
    ['Strategy', 'State', 'Command', 'Mediator'], 1,
    'Behaviour delegated to a current-state object that itself triggers the transition to another state is the State pattern.', [GOF]),
  mk('behavioural-state:A5', 'behavioural-state:B5', 'evaluating', 'evaluate', 'stt-evaluate', 'difficult',
    'Behaviour is delegated to an interchangeable object, and that object itself decides to replace the current one based on events, driving transitions. Is this State or Strategy?',
    ['Strategy — any delegation is Strategy', 'Observer — it notifies dependents', 'State — the behaviour objects drive transitions based on the object’s changing state', 'Template Method — it fixes a skeleton'], 2,
    'When the delegated objects themselves drive transitions based on the object’s evolving state, it is State, not a client-chosen Strategy.', [GOF, MODULE]),
];
const stateB: Form = [
  mk('behavioural-state:B1', 'behavioural-state:A1', 'remembering', 'remember', 'stt-intent', 'easy',
    'The State pattern lets ___.',
    ['many observers be notified of changes', 'algorithms be chosen by the client', 'requests be encapsulated as objects', 'an object change its behaviour as its internal state changes, as if its class changed'], 3,
    'State changes behaviour as internal state changes. The distractors are Observer, Strategy, and Command.', [GOF, MODULE]),
  mk('behavioural-state:B2', 'behavioural-state:A2', 'understanding', 'understand', 'stt-mechanism', 'moderate',
    'What replaces sprawling if/switch-on-status logic in the State pattern?',
    ['Separate state classes that encapsulate state-specific behaviour and transitions', 'A single Singleton holding all logic', 'A strategy chosen once by the client', 'An observer list'], 0,
    'Each state becomes its own class owning that state’s behaviour and transitions, removing the central conditional.', [GOF]),
  mk('behavioural-state:B3', 'behavioural-state:A3', 'applying', 'apply', 'stt-select', 'moderate',
    'A document moves through Draft, Moderation, and Published, where allowed actions and the next step differ per stage. Which pattern cleanly models the changing behaviour?',
    ['Memento', 'State', 'Facade', 'Iterator'], 1,
    'Stage-dependent behaviour and transitions is the State pattern.', [GOF]),
  mk('behavioural-state:B4', 'behavioural-state:A4', 'analyzing', 'analyze', 'stt-identify', 'difficult',
    'Examine:  class LightState { public: virtual void next(TrafficLight&) = 0; };  class Green : public LightState { void next(TrafficLight&) override; /* sets Yellow */ };  class TrafficLight { LightState* st; public: void setState(LightState* s) { st = s; } void change() { st->next(*this); } };  Which pattern is this?',
    ['Strategy', 'Observer', 'State', 'Command'], 2,
    'A context delegating to a current-state object that performs the transition to the next state is the State pattern.', [GOF]),
  mk('behavioural-state:B5', 'behavioural-state:A5', 'evaluating', 'evaluate', 'stt-evaluate', 'difficult',
    'An order object holds a current-state object; calling ship() forwards to the state, which performs the action and swaps the order to the next state. Is “State” correct?',
    ['No — this is Strategy', 'No — State cannot change the object', 'Yes — but only with two states', 'Yes — delegating behaviour to a current-state object that performs transitions is exactly the State pattern'], 3,
    'Delegating to a current-state object that performs the action and the transition is the State pattern.', [GOF]),
];

// ================================ Chain of Responsibility ================================
const chainA: Form = [
  mk('behavioural-chain-of-responsibility:A1', 'behavioural-chain-of-responsibility:B1', 'remembering', 'remember', 'cor-intent', 'easy',
    'What is the intent of the Chain of Responsibility pattern?',
    ['Pass a request along a chain of handlers until one handles it, decoupling sender from receiver', 'Convert interfaces', 'Ensure one instance', 'Share intrinsic state'], 0,
    'Chain of Responsibility forwards a request through handlers until one handles it. The distractors are Adapter, Singleton, and Flyweight.', [GOF, MODULE]),
  mk('behavioural-chain-of-responsibility:A2', 'behavioural-chain-of-responsibility:B2', 'understanding', 'understand', 'cor-decision', 'moderate',
    'What does each handler in the chain decide?',
    ['Which observers to notify', 'Whether to handle the request itself or pass it to the next handler', 'How to convert the request’s interface', 'Which single instance to return'], 1,
    'Each handler either handles the request or forwards it down the chain.', [GOF]),
  mk('behavioural-chain-of-responsibility:A3', 'behavioural-chain-of-responsibility:B3', 'applying', 'apply', 'cor-select', 'moderate',
    'A support ticket should be tried by level-1, then level-2, then a manager, each handling it or passing it on, with the sender unaware who resolves it. Which pattern?',
    ['Mediator', 'Command', 'Chain of Responsibility', 'Observer'], 2,
    'Sequential handle-or-forward with sender/receiver decoupling is Chain of Responsibility.', [GOF]),
  mk('behavioural-chain-of-responsibility:A4', 'behavioural-chain-of-responsibility:B4', 'analyzing', 'analyze', 'cor-identify', 'difficult',
    'Examine:  class Handler { protected: Handler* next = nullptr; public: void setNext(Handler* n) { next = n; } virtual void handle(int lvl) { if (next) next->handle(lvl); } };  class Warn : public Handler { void handle(int lvl) override { if (lvl < 2) log("warn"); else Handler::handle(lvl); } };  Which pattern is this?',
    ['Decorator', 'Composite', 'Observer', 'Chain of Responsibility'], 3,
    'Handlers each holding a next link and choosing to handle or forward a request is Chain of Responsibility.', [GOF]),
  mk('behavioural-chain-of-responsibility:A5', 'behavioural-chain-of-responsibility:B5', 'evaluating', 'evaluate', 'cor-evaluate', 'difficult',
    'Wrappers each hold a “next” and either consume a request or forward it; unlike a wrapper that always augments and delegates, a link may stop the request. Best classified as?',
    ['Chain of Responsibility — handlers conditionally handle or forward, possibly stopping the request', 'Decorator — any chain of wrappers is Decorator', 'Adapter — it converts requests', 'Strategy — it swaps algorithms'], 0,
    'Conditional handle-or-forward (with the option to stop) is Chain of Responsibility; a Decorator always delegates while adding behaviour.', [GOF, MODULE]),
];
const chainB: Form = [
  mk('behavioural-chain-of-responsibility:B1', 'behavioural-chain-of-responsibility:A1', 'remembering', 'remember', 'cor-intent', 'easy',
    'Chain of Responsibility ___.',
    ['captures and restores object state', 'lets a request travel through a series of handlers until one deals with it', 'shares intrinsic state across objects', 'defines an algorithm skeleton'], 1,
    'Chain of Responsibility passes a request through handlers until one handles it. The distractors are Memento, Flyweight, and Template Method.', [GOF, MODULE]),
  mk('behavioural-chain-of-responsibility:B2', 'behavioural-chain-of-responsibility:A2', 'understanding', 'understand', 'cor-decision', 'moderate',
    'What coupling does Chain of Responsibility reduce?',
    ['Between observer and subject', 'Between leaf and composite', 'Between the request’s sender and the specific receiver, since the sender need not know which handler responds', 'Between abstraction and implementation'], 2,
    'The sender is decoupled from the concrete handler that ends up servicing the request.', [GOF]),
  mk('behavioural-chain-of-responsibility:B3', 'behavioural-chain-of-responsibility:A3', 'applying', 'apply', 'cor-select', 'moderate',
    'An HTTP request passes through authentication, then rate-limiting, then logging middleware; any stage may handle/reject it or pass it on. Which pattern?',
    ['Visitor', 'Facade', 'Template Method', 'Chain of Responsibility'], 3,
    'A pipeline of handlers that each handle-or-forward a request is Chain of Responsibility.', [GOF]),
  mk('behavioural-chain-of-responsibility:B4', 'behavioural-chain-of-responsibility:A4', 'analyzing', 'analyze', 'cor-identify', 'difficult',
    'Examine:  class Approver { protected: Approver* next = nullptr; public: void setNext(Approver* n) { next = n; } virtual void approve(int amt) { if (next) next->approve(amt); } };  class Manager : public Approver { void approve(int amt) override { if (amt <= 1000) sign(); else Approver::approve(amt); } };  Which pattern is this?',
    ['Chain of Responsibility', 'Mediator', 'Command', 'Observer'], 0,
    'Linked approvers each handling within their limit or forwarding to the next is Chain of Responsibility.', [GOF]),
  mk('behavioural-chain-of-responsibility:B5', 'behavioural-chain-of-responsibility:A5', 'evaluating', 'evaluate', 'cor-evaluate', 'difficult',
    'Each validator holds a reference to the next; on failure it stops, otherwise it calls the next, and the caller doesn’t know which validator ran. Is “Chain of Responsibility” correct?',
    ['No — this is Decorator', 'Yes — linked handlers that each handle-or-pass while hiding the receiver from the sender is Chain of Responsibility', 'No — the sender must know the handler', 'Yes — but only with one handler'], 1,
    'Linked handlers that each handle-or-forward while the sender stays unaware of the receiver is Chain of Responsibility.', [GOF]),
];

// ================================ Mediator ================================
const mediatorA: Form = [
  mk('behavioural-mediator:A1', 'behavioural-mediator:B1', 'remembering', 'remember', 'md-intent', 'easy',
    'What is the intent of the Mediator pattern?',
    ['Convert interfaces', 'Traverse a collection', 'Define an object that centralises and coordinates communication between a set of objects so they don’t refer to each other directly', 'Share intrinsic state'], 2,
    'Mediator centralises inter-object communication. The distractors are Adapter, Iterator, and Flyweight.', [GOF, MODULE]),
  mk('behavioural-mediator:A2', 'behavioural-mediator:B2', 'understanding', 'understand', 'md-coupling', 'moderate',
    'How does a Mediator reduce coupling among colleague objects?',
    ['By merging all colleagues into one class', 'By making each colleague a Singleton', 'By converting their interfaces', 'Colleagues talk only to the mediator, which routes interactions, instead of holding references to each other'], 3,
    'Colleagues communicate through the mediator rather than referencing each other, cutting many-to-many coupling.', [GOF]),
  mk('behavioural-mediator:A3', 'behavioural-mediator:B3', 'applying', 'apply', 'md-select', 'moderate',
    'A dialog’s many widgets (list, text box, button) must enable/disable each other in complex ways; you want to avoid each widget referencing all the others. Which pattern?',
    ['Mediator', 'Observer', 'Composite', 'Command'], 0,
    'Centralising the widgets’ mutual interactions in one coordinator is Mediator. Observer would be one-way notification.', [GOF]),
  mk('behavioural-mediator:A4', 'behavioural-mediator:B4', 'analyzing', 'analyze', 'md-identify', 'difficult',
    'Examine:  class Mediator { public: virtual void notify(Widget* sender, std::string ev) = 0; };  class Dialog : public Mediator { Button* ok; ListBox* list; public: void notify(Widget* s, std::string ev) override { if (s == list && ev == "select") ok->enable(); } };  Which pattern is this?',
    ['Observer', 'Mediator', 'Facade', 'Visitor'], 1,
    'A hub that receives events from colleagues and coordinates how others respond is Mediator.', [GOF]),
  mk('behavioural-mediator:A5', 'behavioural-mediator:B5', 'evaluating', 'evaluate', 'md-evaluate', 'difficult',
    'A hub receives events from peer objects and tells other peers how to react, governing their mutual interaction (not one-way notification, not just simplifying a subsystem). Best classified as?',
    ['Observer — it is one-to-many notification', 'Facade — it just simplifies a subsystem', 'Mediator — it centralises and coordinates mutual communication among peers', 'Adapter — it converts interfaces'], 2,
    'Two-way coordination of peers’ mutual interactions is Mediator; Observer is one-way, Facade is one-way subsystem simplification.', [GOF, MODULE]),
];
const mediatorB: Form = [
  mk('behavioural-mediator:B1', 'behavioural-mediator:A1', 'remembering', 'remember', 'md-intent', 'easy',
    'A Mediator object ___.',
    ['captures object state for restore', 'defines interchangeable algorithms', 'passes a request along a chain', 'coordinates interactions among objects so they need not reference each other directly'], 3,
    'Mediator coordinates interactions so colleagues stay decoupled. The distractors are Memento, Strategy, and Chain of Responsibility.', [GOF, MODULE]),
  mk('behavioural-mediator:B2', 'behavioural-mediator:A2', 'understanding', 'understand', 'md-coupling', 'moderate',
    'What problem does Mediator address as the number of interacting objects grows?',
    ['The explosion of direct many-to-many references, which it replaces with a single coordinating hub', 'Slow algorithms, replaced by faster ones', 'Excess memory, solved by sharing state', 'Incompatible interfaces, solved by conversion'], 0,
    'Mediator replaces tangled many-to-many references with communication through one coordinator.', [GOF]),
  mk('behavioural-mediator:B3', 'behavioural-mediator:A3', 'applying', 'apply', 'md-select', 'moderate',
    'In an air-traffic system, planes must not talk to each other directly; a control tower coordinates who lands when. Which pattern does the tower embody?',
    ['Observer', 'Mediator', 'Strategy', 'Iterator'], 1,
    'A central coordinator through which otherwise-decoupled peers interact is Mediator.', [GOF]),
  mk('behavioural-mediator:B4', 'behavioural-mediator:A4', 'analyzing', 'analyze', 'md-identify', 'difficult',
    'Examine:  class ChatRoom { std::vector<User*> users; public: void send(std::string msg, User* from) { for (auto* u : users) if (u != from) u->receive(msg); } };  class User { ChatRoom* room; public: void post(std::string m) { room->send(m, this); } };  Which pattern is this?',
    ['Observer', 'Command', 'Mediator', 'Composite'], 2,
    'Users communicating only through a central ChatRoom that routes messages is Mediator.', [GOF]),
  mk('behavioural-mediator:B5', 'behavioural-mediator:A5', 'evaluating', 'evaluate', 'md-evaluate', 'difficult',
    'Form fields never reference each other; instead each notifies a FormController that decides how the other fields update. Is “Mediator” the right classification?',
    ['No — this is Observer', 'No — Mediator forbids more than two peers', 'Yes — but only for UI code', 'Yes — a central object coordinating mutual interactions among otherwise-decoupled peers is exactly Mediator'], 3,
    'A central coordinator governing peers’ mutual interactions is Mediator.', [GOF]),
];

// ================================ Visitor ================================
const visitorA: Form = [
  mk('behavioural-visitor:A1', 'behavioural-visitor:B1', 'remembering', 'remember', 'vs-intent', 'easy',
    'What is the intent of the Visitor pattern?',
    ['Represent an operation to perform on the elements of an object structure, letting you add new operations without changing the element classes', 'Convert interfaces', 'Ensure one instance', 'Traverse a collection sequentially'], 0,
    'Visitor adds operations over a structure without modifying its element classes. The distractors are Adapter, Singleton, and Iterator.', [GOF, MODULE]),
  mk('behavioural-visitor:A2', 'behavioural-visitor:B2', 'understanding', 'understand', 'vs-mechanism', 'moderate',
    'How does Visitor add a new operation across a class hierarchy without editing those classes?',
    ['By rewriting every element class', 'Each element accepts a visitor and dispatches to the visitor’s method for its type; new operations are new visitor classes', 'By making each element a Singleton', 'By converting each element’s interface'], 1,
    'Elements accept() a visitor and call back the type-specific visit() (double dispatch); a new operation is simply a new visitor.', [GOF]),
  mk('behavioural-visitor:A3', 'behavioural-visitor:B3', 'applying', 'apply', 'vs-select', 'moderate',
    'An AST has many node types; you must add operations like type-check, pretty-print, and optimise without modifying each node class every time. Which pattern?',
    ['Composite', 'Iterator', 'Visitor', 'Strategy'], 2,
    'Adding many operations over a stable element hierarchy without editing elements is Visitor.', [GOF]),
  mk('behavioural-visitor:A4', 'behavioural-visitor:B4', 'analyzing', 'analyze', 'vs-identify', 'difficult',
    'Examine:  class Visitor; class Node { public: virtual void accept(Visitor&) = 0; };  class Number : public Node { public: int v; void accept(Visitor& vi) override; /* vi.visit(*this) */ };  class Visitor { public: virtual void visit(Number&) = 0; virtual void visit(Add&) = 0; };  Which pattern is this?',
    ['Composite', 'Mediator', 'Observer', 'Visitor'], 3,
    'accept()/visit() double dispatch over an element hierarchy is the Visitor pattern.', [GOF]),
  mk('behavioural-visitor:A5', 'behavioural-visitor:B5', 'evaluating', 'evaluate', 'vs-evaluate', 'difficult',
    'A team needs to add many unrelated operations over a stable set of node types and rarely adds new node types. Is Visitor an appropriate choice, and why?',
    ['Yes — Visitor fits stable element types with frequently-added operations, localising each operation in a visitor (though adding new element types is costly)', 'No — Visitor is for adding new element types easily', 'No — Visitor cannot support multiple operations', 'Yes — because Visitor makes adding new node types trivial'], 0,
    'Visitor suits stable element hierarchies with many added operations; its known cost is that new element types require touching every visitor.', [GOF, MODULE]),
];
const visitorB: Form = [
  mk('behavioural-visitor:B1', 'behavioural-visitor:A1', 'remembering', 'remember', 'vs-intent', 'easy',
    'Visitor lets you ___.',
    ['share intrinsic state', 'add new operations over an object structure without modifying the structure’s classes', 'notify many observers', 'encapsulate a request as an object'], 1,
    'Visitor adds operations without modifying element classes. The distractors are Flyweight, Observer, and Command.', [GOF, MODULE]),
  mk('behavioural-visitor:B2', 'behavioural-visitor:A2', 'understanding', 'understand', 'vs-mechanism', 'moderate',
    'What technique lets the correct visitor method run for each concrete element type?',
    ['A global switch on a type tag', 'Reflection at link time', 'Double dispatch — accept() on the element calls the visitor overload specific to that element’s type', 'Copying the element first'], 2,
    'Double dispatch (element.accept → visitor.visit overload) selects the right operation for the concrete element type.', [GOF]),
  mk('behavioural-visitor:B3', 'behavioural-visitor:A3', 'applying', 'apply', 'vs-select', 'moderate',
    'A shape hierarchy needs new operations (area, export-to-SVG, bounding-box) added over time without touching each shape class for every new operation. Which pattern?',
    ['Decorator', 'Bridge', 'State', 'Visitor'], 3,
    'Adding operations over a stable hierarchy without editing element classes is Visitor.', [GOF]),
  mk('behavioural-visitor:B4', 'behavioural-visitor:A4', 'analyzing', 'analyze', 'vs-identify', 'difficult',
    'Examine:  class ShapeVisitor { public: virtual void visit(Circle&) = 0; virtual void visit(Square&) = 0; };  class Shape { public: virtual void accept(ShapeVisitor&) = 0; };  class Circle : public Shape { void accept(ShapeVisitor& v) override { v.visit(*this); } };  Which pattern is this?',
    ['Visitor', 'Composite', 'Strategy', 'Iterator'], 0,
    'accept()/visit() double dispatch over a shape hierarchy is Visitor.', [GOF]),
  mk('behavioural-visitor:B5', 'behavioural-visitor:A5', 'evaluating', 'evaluate', 'vs-evaluate', 'difficult',
    'Adding a new node type to a working Visitor design forces edits to every visitor class. Is this a known and acceptable trade-off of Visitor?',
    ['No — Visitor makes adding element types free', 'Yes — Visitor trades cheap new operations for costly new element types; this is its well-known trade-off, acceptable when element types are stable', 'No — it means the design is wrong', 'Yes — but only in C++'], 1,
    'Visitor’s recognised trade-off: easy new operations, costly new element types — acceptable when the element set is stable.', [GOF, MODULE]),
];

// ================================ Interpreter ================================
const interpreterA: Form = [
  mk('behavioural-interpreter:A1', 'behavioural-interpreter:B1', 'remembering', 'remember', 'in-intent', 'easy',
    'What is the intent of the Interpreter pattern?',
    ['Convert interfaces', 'Share intrinsic state', 'Given a language, define a representation for its grammar and an interpreter that uses the representation to evaluate sentences', 'Provide a surrogate for access'], 2,
    'Interpreter represents a grammar and evaluates sentences in that language. The distractors are Adapter, Flyweight, and Proxy.', [GOF, MODULE]),
  mk('behavioural-interpreter:A2', 'behavioural-interpreter:B2', 'understanding', 'understand', 'in-represent', 'moderate',
    'How are grammar rules typically represented in the Interpreter pattern?',
    ['As a single regex', 'As a global lookup table only', 'As one giant switch statement', 'Each grammar rule becomes a class with an interpret() method; expressions form a tree of these objects'], 3,
    'Each rule maps to a class with interpret(); a sentence is an abstract syntax tree of those rule objects.', [GOF]),
  mk('behavioural-interpreter:A3', 'behavioural-interpreter:B3', 'applying', 'apply', 'in-select', 'moderate',
    'You must evaluate simple boolean expressions like (A AND B) OR C entered by users, composing rules recursively. Which pattern represents and evaluates such a grammar?',
    ['Interpreter', 'Visitor', 'Strategy', 'Composite'], 0,
    'Representing grammar rules as composable evaluable objects is Interpreter. The distractors add operations, swap algorithms, or only model part-whole trees.', [GOF]),
  mk('behavioural-interpreter:A4', 'behavioural-interpreter:B4', 'analyzing', 'analyze', 'in-identify', 'difficult',
    'Examine:  class Expr { public: virtual int eval() = 0; };  class Num : public Expr { int v; public: int eval() override { return v; } };  class Add : public Expr { Expr* l; Expr* r; public: int eval() override { return l->eval() + r->eval(); } };  Which pattern is this?',
    ['Composite', 'Interpreter', 'Iterator', 'Command'], 1,
    'Grammar-rule classes with eval() composed into an expression tree is the Interpreter pattern.', [GOF]),
  mk('behavioural-interpreter:A5', 'behavioural-interpreter:B5', 'evaluating', 'evaluate', 'in-evaluate', 'difficult',
    'A team uses the Interpreter pattern for a complex, performance-critical, full programming language with deeply nested grammar. Is this an appropriate use, and why?',
    ['Yes — Interpreter scales best to large languages', 'Yes — performance is Interpreter’s strength', 'No — Interpreter fits small, simple, stable grammars; large complex ones are better served by parser/compiler tools', 'No — Interpreter cannot evaluate expressions'], 2,
    'Interpreter suits small, stable grammars; for large/complex performance-critical languages it becomes unwieldy and a parser/compiler is preferable.', [GOF, MODULE]),
];
const interpreterB: Form = [
  mk('behavioural-interpreter:B1', 'behavioural-interpreter:A1', 'remembering', 'remember', 'in-intent', 'easy',
    'The Interpreter pattern ___.',
    ['notifies dependents of changes', 'selects interchangeable algorithms', 'captures and restores state', 'defines classes for a grammar’s rules and interprets sentences built from them'], 3,
    'Interpreter defines grammar-rule classes and interprets sentences. The distractors are Observer, Strategy, and Memento.', [GOF, MODULE]),
  mk('behavioural-interpreter:B2', 'behavioural-interpreter:A2', 'understanding', 'understand', 'in-represent', 'moderate',
    'What is the relationship between Interpreter and a tree of expression objects?',
    ['Sentences are represented as an abstract syntax tree of rule objects, and interpretation recurses over that tree', 'Interpretation requires no data structure', 'Each sentence is one flat object', 'The grammar must be stored in a database'], 0,
    'A sentence becomes an AST of rule objects; interpret() recurses over the tree to evaluate it.', [GOF]),
  mk('behavioural-interpreter:B3', 'behavioural-interpreter:A3', 'applying', 'apply', 'in-select', 'moderate',
    'A spreadsheet must parse and evaluate user formulas combining numbers, +, and *, nesting arbitrarily. Which pattern represents each operator/operand as an evaluable node?',
    ['Memento', 'Interpreter', 'Mediator', 'Facade'], 1,
    'Representing each grammar element as a composable evaluable node is Interpreter.', [GOF]),
  mk('behavioural-interpreter:B4', 'behavioural-interpreter:A4', 'analyzing', 'analyze', 'in-identify', 'difficult',
    'Examine:  class Node { public: virtual bool eval(const Ctx&) = 0; };  class Var : public Node { std::string name; public: bool eval(const Ctx& c) override { return c.get(name); } };  class And : public Node { Node* a; Node* b; public: bool eval(const Ctx& c) override { return a->eval(c) && b->eval(c); } };  Which pattern is this?',
    ['Visitor', 'Strategy', 'Interpreter', 'Observer'], 2,
    'Grammar-rule node classes with eval() composed into a tree is the Interpreter pattern.', [GOF]),
  mk('behavioural-interpreter:B5', 'behavioural-interpreter:A5', 'evaluating', 'evaluate', 'in-evaluate', 'difficult',
    'For a tiny, rarely-changing query mini-language, a team models each rule as a class with eval() and composes them into a tree. Is Interpreter a reasonable choice here?',
    ['No — Interpreter never fits query languages', 'No — grammars cannot be trees', 'Yes — but only for arithmetic', 'Yes — Interpreter suits small, stable grammars represented as a tree of rule objects'], 3,
    'A small, stable grammar represented as a tree of rule objects is exactly where Interpreter fits well.', [GOF]),
];

// ================================ Memento ================================
const mementoA: Form = [
  mk('behavioural-memento:A1', 'behavioural-memento:B1', 'remembering', 'remember', 'mm-intent', 'easy',
    'What is the intent of the Memento pattern?',
    ['Capture and externalise an object’s internal state so it can be restored later, without violating encapsulation', 'Convert interfaces', 'Ensure one instance', 'Share intrinsic state across objects'], 0,
    'Memento captures and restores state without breaking encapsulation. The distractors are Adapter, Singleton, and Flyweight.', [GOF, MODULE]),
  mk('behavioural-memento:A2', 'behavioural-memento:B2', 'understanding', 'understand', 'mm-encaps', 'moderate',
    'How does Memento preserve encapsulation while saving state?',
    ['It makes all fields public', 'The originator creates an opaque memento that the caretaker stores without inspecting its internals', 'It copies the originator’s class definition', 'It exposes the state through getters to everyone'], 1,
    'The originator builds the memento; the caretaker stores it opaquely, never reading the internals, so encapsulation holds.', [GOF]),
  mk('behavioural-memento:A3', 'behavioural-memento:B3', 'applying', 'apply', 'mm-select', 'moderate',
    'A drawing app needs unlimited undo by snapshotting the canvas state and restoring it later, without exposing the canvas’s private fields. Which pattern?',
    ['Command', 'State', 'Memento', 'Observer'], 2,
    'Snapshotting and restoring state without exposing internals is Memento. Command reifies requests; State models modes.', [GOF]),
  mk('behavioural-memento:A4', 'behavioural-memento:B4', 'analyzing', 'analyze', 'mm-identify', 'difficult',
    'Examine:  class Memento { friend class Editor; std::string state; Memento(std::string s) : state(s) {} };  class Editor { std::string text; public: Memento save() { return Memento(text); } void restore(const Memento& m) { text = m.state; } };  Which pattern is this?',
    ['Command', 'State', 'Prototype', 'Memento'], 3,
    'An originator producing an opaque snapshot object it can later restore from is the Memento pattern.', [GOF]),
  mk('behavioural-memento:A5', 'behavioural-memento:B5', 'evaluating', 'evaluate', 'mm-evaluate', 'difficult',
    'A “snapshot” design exposes all of an object’s internal fields publicly so external code can copy and later write them back. Does this satisfy Memento’s intent?',
    ['No — Memento must capture state without breaking encapsulation; exposing internals publicly violates that', 'Yes — public fields are required for Memento', 'Yes — any save/restore is Memento', 'No — Memento cannot restore state'], 0,
    'Memento’s point is capturing state without breaking encapsulation; publicly exposing internals defeats it.', [GOF, MODULE]),
];
const mementoB: Form = [
  mk('behavioural-memento:B1', 'behavioural-memento:A1', 'remembering', 'remember', 'mm-intent', 'easy',
    'Memento is used to ___.',
    ['coordinate communication between objects', 'save and later restore an object’s state without exposing its internal representation', 'define interchangeable algorithms', 'traverse a collection'], 1,
    'Memento saves and restores state without exposing internals. The distractors are Mediator, Strategy, and Iterator.', [GOF, MODULE]),
  mk('behavioural-memento:B2', 'behavioural-memento:A2', 'understanding', 'understand', 'mm-encaps', 'moderate',
    'What are the three roles in the Memento pattern?',
    ['Subject, Observer, Client', 'Context, State, Transition', 'Originator (owns state), Memento (stores a snapshot), and Caretaker (keeps mementos without inspecting them)', 'Abstraction, Implementor, Bridge'], 2,
    'Memento’s roles are Originator, Memento, and Caretaker; the caretaker stores snapshots opaquely.', [GOF]),
  mk('behavioural-memento:B3', 'behavioural-memento:A3', 'applying', 'apply', 'mm-select', 'moderate',
    'A game must let players save a checkpoint and reload exactly that state later, while keeping the player object’s fields private. Which pattern?',
    ['Command', 'Prototype', 'State', 'Memento'], 3,
    'Saving and restoring an object’s state without exposing its fields is Memento.', [GOF]),
  mk('behavioural-memento:B4', 'behavioural-memento:A4', 'analyzing', 'analyze', 'mm-identify', 'difficult',
    'Examine:  class Snapshot { friend class Game; int hp; int level; Snapshot(int h, int l) : hp(h), level(l) {} };  class Game { int hp; int level; public: Snapshot save() { return Snapshot(hp, level); } void load(const Snapshot& s) { hp = s.hp; level = s.level; } };  Which pattern is this?',
    ['Memento', 'Command', 'Observer', 'Strategy'], 0,
    'An originator producing/consuming an opaque state snapshot is the Memento pattern.', [GOF]),
  mk('behavioural-memento:B5', 'behavioural-memento:A5', 'evaluating', 'evaluate', 'mm-evaluate', 'difficult',
    'An object returns an opaque token capturing its current state; a history manager stores tokens and hands one back to restore the object, never reading the token’s contents. Is “Memento” correct?',
    ['No — this is Command', 'Yes — an opaque state object stored by a caretaker and used to restore the object, without inspection, is Memento', 'No — Memento requires public state', 'Yes — but only for games'], 1,
    'An opaque snapshot held by a caretaker and used to restore the originator, without inspection, is the Memento pattern.', [GOF]),
];

export const BEHAVIOURAL_ASSESSMENT_FORMS: Record<string, { A: Form; B: Form }> = {
  'behavioural-strategy': { A: strategyA, B: strategyB },
  'behavioural-observer': { A: observerA, B: observerB },
  'behavioural-iterator': { A: iteratorA, B: iteratorB },
  'behavioural-command': { A: commandA, B: commandB },
  'behavioural-template-method': { A: templateA, B: templateB },
  'behavioural-state': { A: stateA, B: stateB },
  'behavioural-chain-of-responsibility': { A: chainA, B: chainB },
  'behavioural-mediator': { A: mediatorA, B: mediatorB },
  'behavioural-visitor': { A: visitorA, B: visitorB },
  'behavioural-interpreter': { A: interpreterA, B: interpreterB },
  'behavioural-memento': { A: mementoA, B: mementoB },
};
